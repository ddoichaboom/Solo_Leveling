# 4월 26일 진행 사항

> 브랜치: `feature/Camera`
> 선행: [4월 25일 진행 사항](./4월%2025일%20진행%20사항.md)

---

## 1. 요지

4/25 §3 (Dash 시 마우스 Y 둔화) 의 **가장 빈번한 재현 시나리오**(WASD + Space 시 카메라/플레이어 yaw 차이가 π 근처) 를 구조 차원에서 차단했다. Ch.6 카메라 패치(Update/Late_Update 분리 + q-direct basis) 는 여전히 필요하지만 우선순위 하락.

핵심 변경 3개:
1. `CTransform_3D::Rotate_Toward_XZ` — 범용 회전 보간 API 신설 (이동 회전과 즉시 회전 양쪽이 공유)
2. `CPlayer::Face_DirectionImmediately` — Dash 진입 직전 즉시 회전용 진입점
3. **Dash 분기 정책 변경** — 키-조합(`W+SPACE`/`S+SPACE`) 기반 → **이동 입력 유무(`bHasMoveIntent`)** 기반

---

## 2. `CTransform_3D::Rotate_Toward_XZ` 신설

### 2.1 동기

기존 `Apply_MoveIntent` 내부에 약 60줄의 회전 보간 로직(현재 yaw → 목표 yaw, 최단 경로 정규화, rad/s 클램프, RIGHT/UP/LOOK 재구성) 이 인라인. Dash 진입 시 "즉시 회전" 도 같은 수학을 필요로 했다 → 두 곳에서 코드 중복 발생 위험.

### 2.2 결정

- **Engine 측 (`CTransform_3D`) 에 두기로 확정**
- 회전 한도 파라미터(`fMaxRadiansThisFrame`) 로 보간/즉시 양쪽 커버
  - 이동 회전: `RotationPerSec * fTimeDelta` 전달 → 보간
  - 즉시 회전: `XM_PI` 전달 → 한 프레임에 어떤 각도라도 도달
- XZ 평면 한정 (Y 축 회전, Pitch/Roll 미변경) — 캐릭터 root 회전 전용 의미 명시

### 2.3 시그니처

```cpp
// Engine/Public/Transform_3D.h
void Rotate_Toward_XZ(_fvector vDirWorld, _float fMaxRadiansThisFrame);
```

### 2.4 핵심 로직 (Engine/Private/Transform_3D.cpp)

1. `vDirWorld` XZ 길이 검사 → 0 이면 no-op
2. `atan2f(dirX, dirZ)` 로 목표 yaw, 현재 LOOK 으로 현재 yaw
3. `fDiff` `[-π, π]` 정규화 (최단 경로)
4. `fMaxRadiansThisFrame` 로 클램프
5. 기존 RIGHT/UP/LOOK **scale 길이 보존** 후 새 yaw 로 RIGHT/LOOK 재구성, UP 은 (0,1,0)

---

## 3. `CPlayer::Face_DirectionImmediately` 신설

```cpp
void CPlayer::Face_DirectionImmediately(const _float3& vDirWorld)
{
    if (nullptr == m_pTransformCom) return;
    static_cast<CTransform_3D*>(m_pTransformCom)
        ->Rotate_Toward_XZ(XMLoadFloat3(&vDirWorld), XM_PI);
}
```

**위치 결정**: `CPlayer` 측에 두는 것으로 확정. 이유:
- 의미가 "이동 입력 방향으로 즉시 facing 정렬" — 캐릭터 게임플레이 문맥
- Transform 은 **수학 primitive (`Rotate_Toward_XZ`)** 만 제공, **언제 호출할지 (Dash 진입 직전)** 의 정책은 Player/StateMachine 책임이라는 분리 유지

---

## 4. Dash 분기 정책 변경 — 이동 입력 유무 기반

### 4.1 변경 전후

| 시나리오 | 변경 전 | 변경 후 |
|---|---|---|
| `W + SPACE` | DASH (전방) | DASH (이동 방향 = 카메라 정면) |
| `A + SPACE` | DASH | DASH (이동 방향 = 카메라 좌측) |
| `D + SPACE` | DASH | DASH (이동 방향 = 카메라 우측) |
| `S + SPACE` | **BACK_DASH** | **DASH** (180° 즉시 회전 후 후방) |
| `S + D + SPACE` | 모호 (forward 음수) | DASH (대각 후방으로 회전) |
| SPACE 단독 (WASD 안 누름) | (정의 없음 / forward 0) | **BACK_DASH** |
| 공격 중 SPACE | BACK_DASH | BACK_DASH (변경 없음) |

### 4.2 구현 (Player_StateMachine.cpp `Update_LocoMotion`)

```cpp
if (true == Intent.bDashRequested && nullptr != m_pOwner)
{
    if (true == m_pOwner->Can_ConsumeDashCharge())
    {
        const CHARACTER_ACTION eDashAction = (false == Intent.bHasMoveIntent)
            ? CHARACTER_ACTION::BACK_DASH
            : CHARACTER_ACTION::DASH;

        // DASH 진입 직전 카메라-상대 이동 방향으로 즉시 회전
        if (CHARACTER_ACTION::DASH == eDashAction)
            m_pOwner->Face_DirectionImmediately(Intent.vMoveDirWorld);

        if (true == Try_Transition(ETOUI(eDashAction)))
            m_pOwner->Consume_DashCharge();
    }
}
```

### 4.3 4/25 §3 잔존 이슈 부분 해소 검토

**4/25 §3 원인 요약**:
- `Player::Update` 가 직전 프레임 카메라 yaw 를 읽음 (한 프레임 lag)
- 평소엔 무시 가능, 그러나 카메라 forward ↔ player forward 차이가 **π 근처** 일 때 stale 오차가 증폭됨
- Dash 의 큰 위치 변화가 mismatch 를 카메라에 강하게 남김

**오늘 변경의 효과**:
- 이전 `S + SPACE = BACK_DASH` 케이스가 가장 빈번한 "각도 차 π 근처" 진입 경로였음 (정면 카메라 ↔ 후방 BACK_DASH)
- 새 정책: `S + SPACE` 시 **즉시 180° 회전 후 DASH** → DASH 시작 시점에 player look 이 이미 이동 방향과 정렬됨 → π 차이 자체가 발생하지 않음
- WASD + SPACE 모든 케이스에서 `Face_DirectionImmediately` 가 player look 을 이동 방향과 정렬하므로 stale lag 의 증폭 조건 제거

**여전히 남는 케이스**:
- SPACE 단독 (BACK_DASH) — facing 유지 정책이라 카메라/플레이어 yaw 차이는 입력 시점 그대로 유지됨. 다만 BACK_DASH 는 "정면 바라보기" 가 의도된 동작이므로 § 3 의 "마우스 Y 둔화" 가 체감 버그라기보단 정상 거동에 가까움

→ **잔존 이슈 #4 (Dash 방향 판정) 사실상 해소**. Ch.6 (카메라 stale + basis) 패치는 BACK_DASH 케이스에서 마우스 Y 응답 끊김이 체감되는지 여부에 따라 진행 여부 재판단.

---

## 5. 갱신된 명세서

| 파일 | 변경 |
|---|---|
| `단계3_Player_세부계획.md` §4.4.1 | SPACE 문맥 해석 의사코드 갱신 + 정책 변경 배경 박스 추가 |
| `단계3_Player_세부계획.md` §1.4 | "키 조합" → "이동 입력 유무 기반" |
| `단계3_Player_세부계획.md` §7.4.1 | 다이어그램 라인 갱신 |
| `단계3_Player_세부계획.md` §9.2 Step B | 설명 갱신 |
| `단계3_Player_세부계획.md` §10 체크리스트 | `W+SPACE`/`S+SPACE` 항목 → `WASD+SPACE`/`SPACE 단독`/`S+SPACE 는 DASH` |
| `통합_구현계획_v2.md` §4.1 키 매핑 | `WASD+SPACE`/`SPACE 단독` 분기 + `S+SPACE` 도 DASH 명시 |
| `통합_구현계획_v2.md` §4.1 Intent 예시 | `Intent::DashForward`/`Backward` 분기 기준 갱신 + `Face_DirectionImmediately` 호출 명시 |

---

## 6. 잔여 Task (오늘 시점)

| # | 항목 | 상태 |
|---|---|---|
| 1 | 명세서 갱신 — Dash 정책 변경 반영 | ✅ 완료 (본 문서 §5) |
| 2 | 4월 26일 진행 사항.md 신설 | ✅ 본 문서 |
| 3 | 4월 25일 §4 잔여 Task #4 (Dash 방향 판정) | ✅ 사실상 해소 (본 문서 §4.3) |
| 4 | Ch.6 카메라 stale frame + basis 재구성 패치 | ✅ close (재해석) — §8.5 K-2 체감 무 / **실제 카메라 어색함의 원인은 별건이었음**: SpringArm 의 Pitch-Yaw **합성 순서** 버그. §10 에서 진단·수정. 4/25 §3.2 의 "basis Cross degenerate" 추정은 **틀린 가설** (Pitch clamp 1.0 rad 으로 90° 미달, Cross 항상 정상) |
| 5 | `Player_StateMachine.cpp:92` 연산자 우선순위 검토 | ✅ 완료 (본 문서 §7) |
| 6 | Dash/이동 회귀 테스트 시나리오 정리 | ✅ 완료 (본 문서 §8) |

---

## 7. `Player_StateMachine.cpp` 연산자 우선순위 수정 (Task #5)

### 7.1 원본 식 (수정 전)

```cpp
if ((CHARACTER_ACTION::DASH == eFinished ||
    CHARACTER_ACTION::BACK_DASH == eFinished &&
    true == m_bLastHasMoveIntent))
```

`&&` 가 `||` 보다 우선 → 실제 평가는 `DASH || (BACK_DASH && bLastHasMoveIntent)` 로 됨.

### 7.2 증상

- `DASH` 종료 → WASD 안 눌렸어도 RUN 전이 시도 (§7.4 R-1 위반)
- IDLE/RUN priority 동률 0 → AutoReturn 이 IDLE 로 보낸 직후 RUN 으로 덮어쓰기 → 다음 프레임 IDLE 복귀
- 결과: **한 프레임 RUN 애니메이션 플래시** 가능 (비대칭 거동의 정체)

### 7.3 수정 후

```cpp
const _bool bDashFamily =
    (CHARACTER_ACTION::DASH == eFinished) ||
    (CHARACTER_ACTION::BACK_DASH == eFinished);

if (bDashFamily && true == m_bLastHasMoveIntent)
{
    Try_Transition(ETOUI(CHARACTER_ACTION::RUN));
}
```

비교 결과를 명시 변수로 분리 → 우선순위 함정 회피 + 의도 자가-문서화. 빌드/체감 확인 완료.

---

## 8. Dash / 이동 회귀 테스트 시나리오 (Task #6)

오늘 변경(Rotate_Toward_XZ, Face_DirectionImmediately, Dash 분기 정책, RUN 승격 조건 수정) 의 회귀 방지용. 매 변경 후 본 시나리오 전체 통과 확인.

### 8.1 기본 이동 (Style C 회전 이동)

| # | 입력 | 기대 결과 |
|---|---|---|
| M-1 | `W` 만 | 카메라 정면으로 회전하며 전진. 회전 진행 중 동시 이동 |
| M-2 | `S` 만 | 카메라 후면으로 180° 보간 회전 (스냅 아님) 후 전진 |
| M-3 | `A` / `D` 만 | 좌/우로 회전하며 달림 (스트레이프 아님) |
| M-4 | `W` 유지 중 `S` 로 전환 | 원호 궤적, 스냅 회전 없음 |
| M-5 | WASD 뗌 | IDLE 즉시 복귀 |

### 8.2 Dash 분기 (이동 입력 유무 기반 — 4/26 신규 정책)

| # | 입력 (시작 상태 IDLE/WALK/RUN) | 기대 결과 |
|---|---|---|
| D-1 | `W` 홀드 + SPACE | DASH 진입. 진입 직전 카메라 정면으로 즉시 회전 |
| D-2 | `S` 홀드 + SPACE | DASH 진입 (BACK_DASH 아님). 즉시 180° 회전 후 후방 DASH |
| D-3 | `A` 홀드 + SPACE | DASH 진입. 카메라 좌측으로 즉시 회전 |
| D-4 | `S + D` 홀드 + SPACE | DASH 진입. 대각 후방으로 즉시 회전 |
| D-5 | SPACE 단독 (WASD 안 누름) | BACK_DASH 진입. facing 유지 |
| D-6 | 공격 중 SPACE | BACK_DASH (이동 입력 무관, §4.4.1 변경 없음) |
| D-7 | DASH 도중 SPACE 재입력 (차지 ≥ 1) | 즉시 재진입 (§7.5 연타 허용) |

### 8.3 Dash 차지 시스템

| # | 조건 | 기대 결과 |
|---|---|---|
| C-1 | 게임 시작 시점 | 차지 3 보유 |
| C-2 | SPACE 1회 입력 | 차지 1 소비 (`Try_Transition` 성공 시에만) |
| C-3 | 차지 0 + SPACE | DASH/BACK_DASH 진입 안 함 |
| C-4 | 차지 풀(3) 상태 + 시간 경과 | 타이머는 계속 진행 (1 소비 시 즉시 다음 +1 까지 단축) |
| C-5 | 차지 0 → 3.0s 대기 | 차지 +1 |

### 8.4 RUN 승격 (Task #5 수정 후)

| # | 입력 | 기대 결과 |
|---|---|---|
| R-1 | DASH 종료 시점 WASD 홀드 중 | 즉시 RUN 승격 |
| R-2 | DASH 종료 시점 WASD 안 누름 | IDLE. **한 프레임 RUN 플래시 없음** (Task #5 수정 효과) |
| R-3 | BACK_DASH 종료 시점 WASD 홀드 중 | 즉시 RUN 승격 |
| R-4 | BACK_DASH 종료 시점 WASD 안 누름 | IDLE. RUN 플래시 없음 |
| R-5 | RUN 중 WASD 뗌 | IDLE 즉시 복귀 (WALK 아님) |
| R-6 | RUN 중 방향 급변경 | RUN 유지, 회전만 |
| R-7 | RUN 중 SPACE | DASH (차지 ≥ 1 시) |

### 8.5 카메라/플레이어 정합성 (4/25 §3 잔존 이슈 부분 해소 검증)

| # | 시나리오 | 기대 결과 |
|---|---|---|
| K-1 | 정면 바라보기 (player forward ↔ camera forward 정반대) 상태 + `S+SPACE` | DASH 진입 시 즉시 180° 회전 → 마우스 Y 응답 둔화 **없음** |
| K-2 | 정면 바라보기 상태 + SPACE 단독 | BACK_DASH 진입 후 마우스 Y 응답 — 둔화 체감 시 Ch.6 패치 진행 (Task #4) |
| K-3 | DASH 직후 마우스 X 흔들지 않고 마우스 Y만 입력 | 즉시 응답 (D-1~D-4 모든 케이스에서) |

### 8.6 회귀 방지 (기존 동작 유지 확인)

| # | 항목 | 기대 결과 |
|---|---|---|
| Z-1 | GAMEPLAY 진입 / 맵 / Player 스폰 | Step 2.5 회귀 없음 |
| Z-2 | Camera_Follow SpringArm 마우스 X/Y 회전 | 4/25 패치 효과 유지 (Roll 누적 없음, Pitch 클램프 정확) |
| Z-3 | WALK 속도 vs RUN 속도 | `m_fSpeedCoeff` 1.0 / 1.8 분리 정상 |

---

## 9. 다음 세션 시작 시 체크리스트

1. `feature/Camera` 브랜치 + 본 변경(빌드 통과) 상태 확인
2. main 머지 시점 결정 (4/25 §4 #1 carryover) — feature/Camera 작업이 §10 fix 까지 마무리되었으므로 머지 진행 가능

---

## 10. SpringArm Pitch-Yaw 합성 순서 fix (Task #4 재해석)

### 10.1 증상 재정의

4/25 §3 에 "Dash 직후 마우스 Y 둔화" 로 적었던 증상은 **사실 Dash 와 무관** 했음. 별개의 카메라 거동 이상이 누적된 거였고, 사용자가 추가 검토 요청한 후 정확히 재현/특정됨:

- Pitch 끝까지 끌어붙인 상태에서 마우스 X 만 흔들면 화면 상하 각도(Look.y) 가 같이 변함
- 어떤 Yaw 구간(±90° 부근)에서는 마우스 Y 의 실제 효과 범위가 처음 설정한 `[fPitchMin, fPitchMax]` 보다 훨씬 좁아진 느낌
- Yaw ±180° (뒤돌아본 상태) 에서 마우스 Y 가 상하 반전된 것처럼 동작

### 10.2 가설 1 (틀림) — basis Cross degenerate

4/25 §3.2 의 추정: `Camera_Follow::Apply_SpringArmToTransform` 의 `Cross(worldUp, look)` 가 Pitch ±90° 근처에서 degenerate 되어 RIGHT/UP 가 망가짐.

검증용 직교성/단위길이 로깅 (`[BasisBroken]`) 추가 후 실행 → **출력 0건**.

이유: `fPitchMax = 1.0 rad ≈ 57°`, `fPitchMin = -1.4 rad ≈ -80°`. Pitch 가 90° 에 못 가게 클램프되어 있어 Cross 가 항상 정상 직교. 이 가설은 **틀림**.

### 10.3 가설 2 (확정) — Pitch-Yaw 합성 순서

`SpringArm.cpp:108` (수정 전):
```cpp
_vector q = XMQuaternionMultiply(qYaw, qPitch);
```

DirectXMath 의 `XMQuaternionMultiply(A, B)` 는 math 표기로 **`B * A`** (인자 순서 반대 — 함정).

→ math 식: `qPitch * qYaw`. `XMVector3Rotate(v, q)` = `q * v * q⁻¹` 이므로:
- 안쪽: qYaw * v * qYaw⁻¹ — **Yaw 가 먼저** 적용 (월드 Y 축)
- 바깥쪽: qPitch 가 그 결과에 적용 — **Pitch 가 월드 X 축으로** 들어감

`v = (0,0,1)` 적용 결과:
```
Look.x =  cos(Pitch) · sin(Yaw)
Look.y = -sin(Pitch) · cos(Yaw)     ← Yaw 와 결합
Look.z =  cos(Pitch) · cos(Yaw)
```

**검증 로깅** (`[Pitch-Yaw 결합]`, expected = `-sinf(m_fPitch)`, Yaw 무관해야 정상):

| Yaw | Pitch | expected | actual Look.y | actual ÷ expected |
|---|---|---|---|---|
| 0.600 | 0.562 | -0.5329 | -0.4398 | 0.825 ≈ cos(0.600) |
| 0.825 | -1.100 | 0.8912 | 0.6047 | 0.679 ≈ cos(0.825) |
| -1.560 | -1.100 | 0.8912 | 0.0096 | ≈ 0 = cos(-1.56) |
| 3.132 | -1.100 | 0.8912 | -0.8912 | -1 ≈ cos(3.132) |

전 케이스 `Look.y_actual = -sin(Pitch) · cos(Yaw)` 식과 1:1 일치. 가설 확정.

증상이 모두 이 식으로 설명됨:
- Yaw=0 부근: cos(Yaw)≈1 → 정상
- Yaw=±π/2: cos≈0 → Pitch 가 화면에 거의 안 먹힘 (Y 범위 좁아짐)
- Yaw=±π: cos≈-1 → 부호 반전 (마우스 Y 위/아래 뒤집힘)

### 10.4 Fix

`SpringArm.cpp:77` (Initialize) + `SpringArm.cpp:108` (Update_Rotation) 두 곳 동일 패치:

```cpp
_vector q = XMQuaternionMultiply(qPitch, qYaw);   // 기존: (qYaw, qPitch)
```

DirectXMath 인자 순서 swap → math 식 `qYaw * qPitch`:
- 안쪽: qPitch * v * qPitch⁻¹ — Pitch 가 먼저 (v 의 base 프레임 = 월드 → 월드 X 축)
- 바깥쪽: qYaw — 월드 Y 축으로 회전 (Y 좌표 보존)

결과:
```
Look.x =  cos(Pitch) · sin(Yaw)
Look.y = -sin(Pitch)              ← Yaw 무관
Look.z =  cos(Pitch) · cos(Yaw)
```

수정 적용 후 `[Pitch-Yaw 결합]` 로깅 **출력 0건** 확인 → 디버그 블록 제거.

### 10.5 학습 정리 — "좌=월드 / 우=로컬" 규칙의 문맥

4/25 학습 페이지 Ch.4 ("좌측=월드 / 우측=지역") 규칙은 **누적 (accumulation) 맥락** 에서:
- `q_new = Δq * q_old` (math, **좌측 곱**) → Δq 의 축은 **월드** 으로 해석
- `q_new = q_old * Δq` (math, **우측 곱**) → Δq 의 축은 **로컬** 으로 해석

이 규칙은 **여전히 유효**. 다만 이번 케이스는 누적이 아니라 **2-quat 합성** (매 프레임 Yaw·Pitch 두 회전을 처음부터 묶음).

같은 규칙을 합성에도 적용 가능 — 둘 중 하나를 "기존" 으로 간주:

| 코드 | math 식 | 좌 (월드) | 우 (로컬) | Pitch 의 실제 축 | 결과 |
|---|---|---|---|---|---|
| `XMQuaternionMultiply(qYaw, qPitch)` (버그) | `qPitch * qYaw` | qPitch | qYaw | **월드 X** | Yaw 와 결합 |
| `XMQuaternionMultiply(qPitch, qYaw)` (fix) | `qYaw * qPitch` | qYaw | qPitch | **로컬 X (= 카메라 right)** | 분리 ✓ |

Yaw 는 항상 월드 Y 축이 맞고, Pitch 는 카메라가 Yaw 만큼 돈 후의 로컬 right 축이어야 자연스러운 yaw-pitch 카메라가 됨. 4/25 재구성 패치 시점에 합성 순서를 잘못 잡은 게 오늘 드러난 것 — **재구성식 자체는 옳고, 그 안의 좌/우 슬롯 배치가 거꾸로였음**.

### 10.6 함정 (다음에 같은 실수 안 하기)

`XMQuaternionMultiply(A, B)` → math `B * A`. **DirectXMath 호출 시 첫 인자가 math 의 오른쪽 슬롯 (= 로컬 슬롯)**.

판단할 때는 항상:
1. DirectXMath 호출을 math 표기로 펼친 후
2. `q * v * q⁻¹` 의 안쪽부터 바깥쪽 순으로 v 가 회전되는 순서를 확인
3. 좌/우 슬롯 = 월드/로컬 규칙 적용

---

## 11. Step C 사전 점검 → OverDrive 모델 도입 시도 (저녁 작업)

### 11.1 Step C 사전 점검 (Task #7 완료)

`CharacterAnimTable.cpp` / `Client_Enum.h` 검토 결과:

**Enum 누락**: `LINK_ATTACK`, `CHAIN_SMASH` 미정의 → END 직전에 추가 (실제 적용은 모델 안정화 후)

**ERank AnimTable 잘못된 매핑**:
- Line 28: `SKILL_02 / ONE_HAND_SWORD / "GS_ChainSmash_01_OneHandSword"` → `CHAIN_SMASH` 로 재매핑 권장

**미등록 액션** (Step C 진입 시 추가):
- LINK_ATTACK 클립 — ERank `.bin` 에 없음. OverDrive `.bin` 으로 교체 후 확인 필요
- CHAIN_SMASH — ERank Line 28 재매핑 + OverDrive 의 무기별 변형 (`GS_ChainSmash_01_*`)

**결정사항**: Step C 진입 전 OverDrive 모델 교체 선행. 미흡한 모델로 임시 매핑 후 재작업 비효율.

### 11.2 OverDrive 모델 1차 도입 (실패 → 수정)

`SungJinWoo_OverDrive.bin` (179 애니, 322 본, v2) 도입 시도. AnimSet/AnimTable/Loader/EditorApp 분리 작성. 실행 시 MSG_BOX 연속 발생 — 원인 4건 순차 처리:

#### 11.2.1 `Get_SungJinWooOverDriveAnimTable()` nullptr 반환 (치명적)

```cpp
// 버그
const CHARACTER_ANIM_TABLE_DESC* Client::Get_SungJinWooOverDriveAnimTable()
{
    return nullptr;   // ← &s_SungJinWooOverDriveAnimTable 여야 함
}
```

→ `Find_CharacterAnimTable` nullptr → `Ready_AnimationTable` 실패. Fix 1줄.

#### 11.2.2 프로토타입 태그 이름 불일치

- `Loader.cpp`: `SungJinWooOverDrive` (언더스코어 없음)
- `CharacterAnimTable.cpp` / `Body_Player.cpp` / `EditorApp.cpp`: `SungJinWoo_OverDrive`

→ Loader 측 통일. 단 Editor 실행 시 Loader 안 거치므로 직접 영향 없음 (GameApp 진입 시 문제).

#### 11.2.3 AnimSet 미변경 (가장 헷갈렸던 부분)

모델은 OverDrive 로 바꿨는데 `Body_Player.h::m_eAnimSet` 와 `Player.cpp:205` 의 AnimSet 선택은 `SUNGJINWOO_ERANK` 그대로 → ERank 클립명 ("Normal_Idle", "Walk", ...) 을 OverDrive `.bin` 에서 찾으니 실패.

**Fix**: 두 곳 모두 `SUNGJINWOO_OVERDRIVE` 로 변경.

#### 11.2.4 Body 가 한 점으로 collapse + 머리만 떠있음

(가) **머리만 떠있음** — `Shader_VtxAnimMesh.hlsl:89` 의 `if (vMtrlDiffuse.a < 0.1f) discard;` 가 PBR alpha-packing 과 충돌. Solo Leveling 의 `_CO.a` 채널은 투명도가 아니라 **Roughness/Metallic** 데이터. 대부분 픽셀이 임계 미달로 discard.

→ 임시 fix: 임계값 `0.1f` → `0.01f` (PBR 데이터가 정확히 0인 경우만 discard).

(나) **본체 floor-pin** — bind pose 자체 깨짐 (Idle 첫 프레임부터 의상 계열이 바닥에 박힘). 다음 §11.3 에서 본격 진단.

### 11.3 OverDrive bind pose 깨짐 — Dual Hierarchy 발견

**핵심 진단**: Body mesh 의 165 skin bone 중 29개 (Bone_Skirt*, Bone_Belt*, Bone_Coat*, Bip001 Xtra_Coat*, Bone_Decoration*, Bone_Pocket* 등 의상/장식) 가 `SungJinWoo_Skin_OverDrive_Dyanamic` EMPTY 의 자손. **나머지 136개는 Bip001 체인**.

#### 11.3.1 .bin bone tree dump 결과

```
정상:    Bip001 ← Translate ← PFV ← OverDrive ← RootNode    (136개)
의상:    Bone_Skirt* ← Dyanamic ← PFV ← OverDrive ← RootNode (29개)
                      ↑ Translate 거치지 않음
```

애니메이션의 root motion 이 `Translate` 본 channel 에 baked (Dash Z=-6.33, BackDash Z=+5.00 등) → Bip001 체인은 Translate 따라 이동, **Dyanamic 체인은 안 따라감 → 의상이 origin (=floor) 에 고정**.

#### 11.3.2 원인 — AssetStudio 의 Unity GameObject 구조 그대로 export

Solo Leveling 의 Unity 원본이 의상/장식 본을 SkinnedMeshRenderer 의 bone[] 배열에 별도 GameObject reference 로 운영 (런타임 dynamic bone 시뮬레이션). AssetStudio 가 FBX 로 export 시 Unity GameObject 트리를 그대로 평탄 배치 → Translate armature 와 Dyanamic EMPTY 가 sibling.

#### 11.3.3 사용자 가설 검토

> "Collect_Bones 가 모든 aiNode 를 본으로 수집하는 게 원인"

→ **부분적 사실**. Helper 노드들이 본 배열에 섞이는 건 맞지만 **floor-pin 의 직접 원인은 아님**. parent index 는 보존되므로 hierarchy 자체는 깨지지 않음. 진짜 원인은 **scene graph 자체에서 의상이 Translate 의 sibling** 인 것.

> "skin bone + ancestor only 로 필터링"

→ 좋은 일반 개선이지만 **floor-pin 해결책으론 부족**. Dyanamic 도 ancestor 라 KeepSet 에 포함됨.

### 11.4 해결 시도 — Blender 스크립트로 reparent

#### 11.4.1 옵션 비교

| 옵션 | 작업 | 결과 |
|---|---|---|
| Blender 수동 정리 + 재export | EMPTY → bone 변환 | 가장 깨끗, 손 큼 |
| Converter 측 reparent + LocalTransform 재계산 | Model_Converter.cpp 코드 | 다른 모델 자동, OffsetMatrix 재계산 복잡 |
| **Blender 스크립트 reparent (안 C 채택)** | Dyanamic.parent = Translate (Keep World) | 단순, 검증 빠름 |

채택: **안 C** — Blender Python 으로 `Dyanamic` EMPTY 의 부모를 `Translate` armature 로 변경 (world transform 보존) + FBX 재export.

#### 11.4.2 Reparent 검증 결과 ✅

`SungJinWoo_OverDrive_Fixed.bin` (애니 0개, 3.5MB) 로 검증:

```
Body0 skin bones: 189개 / via_Translate=189 / NOT_via_Translate=0
의상 본 29개도 모두 Translate 체인 편입
```

**hierarchy fix 자체는 완전히 통함**. (skin bone 165 → 189 증가는 Blender import/export 시 leaf bone 처리. 큰 문제 없음)

#### 11.4.3 시각 검증 시도 — 모든 vertex 한 점 collapse

bind pose 만 보려고 OverDrive AnimTable 클립 0개 + `Play_Action(IDLE)` 결과 무시 → 캐릭터가 **한 점에 모임**.

**원인**: 애니 0개 → AnimController Update 안 도는 본 행렬이 영행렬 → vertex × 0 = 0. **bind pose 가 깨진 게 아니라 본 행렬이 갱신되지 않은 상태**. Play_Animation 한 번이라도 돌려야 본이 default pose 로 채워짐. → **본 export 가 결국 필요**.

### 11.5 본 export 차질 — bake_anim 시간

처음에 `bake_anim=True` + `bake_anim_force_startend_keying=True` + 179 액션 전부 → 20분+ CPU 점유. Ctrl+C 중단.

다음 시도: 게임 사용 클립 27개로 필터링 (KEEP set). 진행 중 (사용자 작업으로 인계).

```python
KEEP = {
    "Idle", "Walk", "Run", "Dash", "BackDash",
    "BaseAttack_01", "BaseAttack_02", "BaseAttack_03",
    "CoreAttack_01", "Skill_02", "Stun", "Dead_A", "Breakfall",
    "Damage_A", "Damage_B_Left", "Damage_B_Right", "Damage_C",
    "GS_ChainSmash_01_OneHandSword",
    "Guard_Dagger_Start", "Guard_Dagger_Loop", "Guard_Dagger_End",
    "U_Skill_Crank_01_Start", "U_Skill_Crank_01_End",
    "Skill01_ChainA_01", "Avoid_Dash",
    "Parrying_Dagger_Strong", "Parrying_Dagger_Right",
}
```

### 11.6 OverDrive `.bin` 분석 (1차 dump 결과)

게임 액션 ↔ 클립 후보 매핑 정리 (1차):

| Action | 클립 | RootMotion 거리(\|D\|) |
|---|---|---|
| IDLE | `Idle` (110) | 0 |
| WALK | `Walk` (106) | 0 |
| RUN | `Run` (105) | 0 |
| DASH | `Dash` (94) | 6.33 |
| BACK_DASH | `BackDash` (126) | 5.00 |
| BASIC_ATTACK_01 | `BaseAttack_01` (76) | 1.95 |
| BASIC_ATTACK_02 | `BaseAttack_02` (85) | 2.31 |
| BASIC_ATTACK_03 | `BaseAttack_03` (65) | 1.07 |
| CORE_ATTACK_01 | `CoreAttack_01` (69) | 3.53 |
| CHAIN_SMASH (OneHandSword) | `GS_ChainSmash_01_OneHandSword` (134) | 8.56 |
| BREAKFALL | `Breakfall` (64) | 5.21 |

**Loop 권장**: IDLE/WALK/RUN/GUARD_LOOP/STUN → true. 나머지 1회성 → false.
**RootMotion 권장**: WALK/RUN/IDLE → false (입력 기반). DASH/BACK_DASH/BASIC_ATTACK*/CORE_ATTACK_01/CHAIN_SMASH/BREAKFALL/돌진계 SKILL → true.

**미발견**:
- LINK_ATTACK — OverDrive 에도 명시적 클립 없음. Solo Leveling 원본은 별도 모델/Avatar 에서 제공. 후보: `Skill01_ChainA_01`, `Avoid_Dash`
- GUARD UNARMED — Dagger 변형만 존재 (`Guard_Dagger_*`)

### 11.7 의상 본의 자연스러운 흔들림 (별도 이슈)

Solo Leveling 원본 Unity 의 dynamic bone 시뮬레이션은 게임 엔진 측에서 살리기 어려움. 본 fix 의 목표는 "**의상이 캐릭터 위치엔 같이 있는다**" 까지. 흔들림은 향후 별도 시스템 (Spring/Verlet) 으로 다룰 영역.

### 11.8 weapon prototype 분리 (Codex 검토 — 후속 작업)

`Player::Ready_PartObjects` 에서 무기 1개만 R 소켓에 부착. 좌/우 동시 장착으로 확장 시:

- **CWeapon 인스턴스 2개** (`Weapon_R` / `Weapon_L`) 생성 권장
- WEAPON_DESC 에 `pSocketBoneMatrix` 외에 `pModelPrototypeTag` + `LocalAttachMatrix` 추가
- 같은 모델 좌/우 사용 시 **PreTransform 으로 좌측 회전 X** (프로토타입 1개에 베이크 → 좌/우 공유), **LocalAttachMatrix 로 인스턴스별 회전** ✓
- `Late_Update` 의 `Normalize(SocketMatrix.r[i])` 가 음수 스케일 미러링을 차단하므로 **다른 모델 또는 LocalAttach 회전이 유일한 길**

작업 시점: OverDrive 본 export 안정화 + Step C 진입 후. L 본 이름 (`Prop_Weapon_Dualwield_01_L`) 은 모델 교체 후 Editor Bone List 로 실측 필요.

---

## 12. 잔존 문제 / 다음 세션 진입점

### 12.1 OverDrive 모델 안정화 (선결)

**현재 상태**:
- ✅ AnimSet/AnimTable/Loader/EditorApp 분리 등록 완료
- ✅ 셰이더 alpha-test 임계값 fix (`0.01f`)
- ✅ Blender Dyanamic→Translate reparent 검증 (Fixed.fbx, hierarchy OK)
- ⏳ 27개 액션 본 export 진행 중 (다음 세션)
- ⏳ Editor 변환 + IDLE 시각 검증 (의상 정상 위치 + Dash root motion 따라가는지)
- ⏳ AnimTable 클립 매핑 본 작업 (위 11.6 표 기준)

**임시 회피 코드 — 정상 검증 후 원복**:
- `Body_Player.cpp::Initialize` 의 `Play_Action(IDLE)` 결과 무시 (`if (FAILED(...))` 주석)
- `CharacterAnimTable.cpp` 의 OverDrive 클립 count 0

### 12.2 Step C 진입 (OverDrive 안정화 후)

기존 Task #8~#12 그대로. 모델 안정화 끝나면:

1. **#8 AnimNotify 인프라** — SLMD v3 + Editor Inspector Notify UI + StateMachine Pending Enqueue
2. **#9 BASIC_ATTACK 콤보** — ComboWindow_Open/Close Notify 소비, MOUSE_LB Edge → Pending 큐
3. **#10 LINK_ATTACK / CHAIN_SMASH** — LINK_ATTACK 보류 결정 필요 (모델 클립 부재). CHAIN_SMASH 만 진행
4. **#11 GUARD** — UNARMED 클립 부재 (Dagger 만). 임시로 Dagger 변형 사용 또는 Step D 까지 보류
5. **#12 Step C 검증 + 명세서 갱신**

### 12.3 결정 보류 항목

- **LINK_ATTACK** — 보류 vs `Skill01_ChainA_01`/`Avoid_Dash` 임시 사용
- **GUARD** — Dagger 변형 임시 사용 vs Step D 까지 보류
- **HIT** — `Damage_A/B_Left/B_Right/C` 4종. 기본 1개 vs 방향별 분기
- **U_SKILL Rank** — Crank vs Srank (E랭크 컨셉이면 Crank 권장)
- **§11.8 무기 좌우 동시 장착** — Step C 끝나고 진행 vs 그 이전?

### 12.4 다음 세션 체크리스트

1. Blender 에서 KEEP set 27개 액션만 남기고 본 export (`bake_anim=True`)
2. `_Fixed.fbx` → Editor 에서 `_Fixed.bin` 재변환
3. animation dump 로 27개 정상 들어갔는지 확인
4. `CharacterAnimTable.cpp` OverDrive 클립 매핑 본 작성 (§11.6 표)
5. `Body_Player::Initialize` Play_Action(IDLE) 회피 원복
6. Editor 진입 → IDLE 정상 / Dash root motion / 의상 따라가는지 검증
7. 정상이면 Step C 진입 (Task #8 부터)
8. 셰이더 alpha-test 임계값 변경 (`0.1 → 0.01`) 은 Body/Weapon/Terrain 각 셰이더 개별 영향 — 회귀 확인 (다른 모델 텍스처 정상인지)

### 12.5 학습 / 메모

- **AssetStudio 의 dual hierarchy 패턴** — 다른 Solo Leveling 캐릭터 모델 추출 시 동일 문제 반복 가능. Blender reparent 스크립트는 재사용 가능 (`DYNAMIC_NAME` / `TARGET_NAME` 만 교체)
- **PBR alpha-packing** — `_CO.a` 가 Roughness/Metallic. 본격 PBR 도입 시 셰이더에 `g_Roughness/Metallic` 입력 추가 + `_CO.a` 연결 + alpha-test 별도 마스크 텍스처 활용
- **Blender FBX export 의 bake_anim 비용** — 액션 N 개 × 평균 frame 수 만큼 sample. Solo Leveling 같은 대량 클립 모델은 **반드시 KEEP filter** 후 export
- **anim 0개 .bin 의 위험** — bind pose 시각 검증조차 불가. AnimController 가 본 행렬을 0 으로 두기 때문. 검증용으로라도 최소 1개 클립 필요
