# 통합 구현 계획 v3

> 작성일: 2026-05-05
> 이전 문서:
> - `명세서/통합_구현계획_v2.md` (2026-04-21) — 단계 구조/Track 명세 계승
> - `명세서/단계3_Player_세부계획.md` (2026-04-22~05-05) — Player 단계 잔여만 §1.2 로 흡수
>
> 본 문서는 **Player 1차 마감(이동/공격/가드) 시점의 재출발선**이다. v2 §4 의 단계 구조는 살아 있되, 진입 순서를 사용자 결정에 따라 5개 트랙으로 재배열했다. 각 트랙은 학습+실행을 동시에 하기 위해 **개념/대안/특징/장단점/본 프로젝트 적용** 순으로 서술한다. 결정사항은 대화하며 구체화하는 것이 전제이며, 본 문서 자체가 그 토론장이다.
> 직접 코드 수정은 금지하며, 사용자에게 패치 사항을 코드 블록 (마크다운) 형태로 제공하여 직접 검토한 후 적용할 수 있도록 한다.

---

## 0. 문서 사용법 (한 번에 읽고 시작하기)

본 문서는 **읽고 합의 → 구체화 대화 → 실행 → 갱신** 의 사이클을 따른다.

| 단계 | 행위 | 기록 위치 |
|------|------|-----------|
| 트랙 진입 직전 | 본 문서 §3~§7 의 해당 트랙 읽기 + 미결 항목 합의 | 합의 결과는 본 문서 *에 직접* 추가 (Decision Log 형식) |
| 트랙 진행 중 | 일자별 진행 사항 작성 | `명세서/MM월 DD일 진행 사항.md` |
| Step 완료 | 본 문서 트랙 §의 Step 표에 ✅ 마킹 + 한줄 결과 | 본 문서 |
| 트랙 완료 | 본 문서 §1.1 (완료 스냅샷) 으로 이동 + Decision Log 동결 | 본 문서 |
| 결정 번복 | 옛 결정에 ⚠️ 표기 + 새 결정 추가 (삭제 금지 — 추적성) | 본 문서 |

본 문서가 수업 코드/외부 라이브러리에 비해 **본 프로젝트의 결정**을 우선 기록하는 매체임을 잊지 말 것. 옛 결정도 *왜 바뀌었는지* 가 다음 결정을 정확하게 만든다.

---

## 1. 현재 상태 스냅샷 (2026-05-05)

### 1.1 완료된 영역

**Layer 0 — 공통 인프라 / Editor**
- Engine API 확장 (오브젝트/레이어/컴포넌트 getter), RTTR Registration, Picking, ImGui/ImGuizmo Editor 격리
- 6개 패널 (Viewport/Hierarchy/Inspector/ContentBrowser/Log/Shortcuts), `CPanel_Manager` 순환 참조 해결
- Editor 부팅을 Client 런타임 호스팅으로 전환 (Edit/Play 토글)
- Layer 조작 API (Detach/Attach/Reorder, Move/Reorder/Remove GameObject)

**Layer 1 — 모델/애니메이션 파이프라인**
- Engine: `CModel/CMesh/CMaterial/CBone/CAnimation/CChannel`
- Editor: `CModel_Converter` (FBX → SLMD .bin, v3 쓰기). v2 → v3 자동 마이그레이션 (version 분기 폴백 로드)
- SLMD v3: 퍼-애니메이션 `bIsLoop` / `bUseRootMotion` / `RootBoneName` / `PreTransform` / `AnimNotify[]`
- Inspector: 모델 정보 + 애니메이션 리스트/재생/루프 토글 + Notify 저작 UI + Save Model 버튼
- Editor 프리뷰 분리 (`Begin_Preview / Restart_Preview / End_Preview` + `m_bPreviewMode`)

**Layer 2 — Player 게임플레이 1차 (이동 + 공격 + 가드)**
- 입력 흐름: `CInput_Device` Edge-trigger / `CIntentResolver` (Raw → Intent) / `CStateMachine` 베이스 + `CPlayer_StateMachine`
- 이동: IDLE / WALK / RUN / RUN_FAST / DASH / BACK_DASH + 카메라 Yaw 기준 월드 이동 방향(`vMoveDirWorld`) + 회전 속도 제한 보간 (Style C)
- 공격 콤보: BASIC_ATTACK_01/02/03 Early-Cancel + ComboWindow Notify (LMB 0.18s 입력 버퍼)
- 가드: GUARD_START/LOOP/END Held-기반 + RMB flicker 디바운스 0.10s + Reject(DASH→GUARD)
- AnimNotify 인프라: `INotifyListener::OnNotify(NOTIFY_EVENT)` Listener pass-through (Body→AnimController→Model→Animation::Tick_Notifies, wrap-aware)
- CHARACTER_STATE 의미 분류 (LOCOMOTION/COMBAT/GUARD) + `Body_Player::Resolve_ActionKey` 4단계 폴백 + 자동 갱신

**Layer 2.5 — 추적 카메라 / Style C 마감 (트랙 1, commit `f1da3fc`)**
- `Camera_Follow` + `SpringArm` 런타임 추적 카메라 마감. 평상시 즉시 추적, 벽 충돌 후 거리 복귀에만 Distance Lerp 확장점 유지
- `Layer_Player` 기반 target 자동 검색/재바인딩. Editor 에서 Player 삭제 시 마지막 위치 유지, 재생성 시 추적 재개
- `Camera_Free` 는 Client Loader 프로토타입 유지 + Editor `Bootstrap_EditScene()` 전용 인스턴스. GamePlay 는 `Camera_Follow` 만 생성
- `PLAYER_INTENT_FRAME` 구조 정리: `vMoveAxis` / `bHasMoveIntent` 제거, `vMoveDirWorld` SSOT + `Client_Func.h::Has_MoveIntent()` 로 통일

**Layer 3 — 런타임 테스트베드**
- Client 런타임 경로(`CLevel_GamePlay`) Editor 와 동일 구성 — `Ready_Layer_BackGround` + `Ready_Layer_Player`

### 1.2 Player 1차 마감 후 잔여 (트랙으로 분배)

| 항목 | 보류 사유 | 어느 트랙? |
|------|-----------|-----------|
| Step C-8 (LINK_ATTACK / CHAIN_SMASH) | 스킬 시스템과 함께 처리하기로 사용자 결정 | 트랙 5 (잔여 Player) |
| Step D (무기 스왑 + 쿨다운) | 우선순위 재검토. 스킬 단계로 묶을지 별도일지 미결 | 트랙 5 (잔여 Player) |
| 단계 2-3 애니메이션 블렌딩 재설계 | Freeze A → Lerp → Play B 방식 검토 | 트랙 5 (잔여 Player) |

### 1.3 알려진 후속 (트랙 외부 메모)

- **Guard_Dagger 클립 시각 이슈**: Guard_Dagger_* 클립 한정으로 검이 손을 관통. 클립 자체 캘리브레이션 미스매치로 추정. 메모리: `project_guard_dagger_visual`
- **Camera_Follow target dangling**: 트랙 1에서 `Layer_Player` 자동 검색/재바인딩으로 1차 해결. 트랙 2 Scene I/O 에서 `SceneObjectId/GUID` + Object Lifetime Event 기반 참조로 확장 예정
- **단계 1 맵 로드 잔이슈**: 일부 메시 누락 / 컬링 양면 미적용 / Bind_Material 실패 연쇄. 트랙 4 (충돌) 와 함께 정리 가능

### 1.4 핵심 아키텍처 결정사항 (계승, 변경 없음)

| 항목 | 결정 |
|------|------|
| Assimp / RTTR / ImGui / ImGuizmo | Editor 전용 격리. Engine/Client 무의존 |
| 모델 포맷 | FBX → SLMD v3 .bin (version 분기 + 누락 필드 폴백 패턴) |
| 씬 포맷 | JSON(편집/확인) + 바이너리(런타임) — 트랙 2 에서 구체화 |
| CRT | 전 프로젝트 동적 CRT (`/MDd`, `/MD`) |
| SSOT | Loop/RootMotion/Notify → `.bin`. Action↔Anim 매핑 → Client 테이블. Transform → `m_WorldMatrix` |

---

## 2. 트랙 구성 (사용자 결정, 2026-05-05)

```
Player 1차 (완료)
   │
   ▼
┌────────────────────────────────────────────────────┐
│ 트랙 1: 추적 카메라 마감 (Step E + Camera 정비)   │   작은 트랙 — 1~2 세션 예상
└────────────────────────────────────────────────────┘
   │
   ▼
┌────────────────────────────────────────────────────┐
│ 트랙 2: NavMesh + Scene 저장/로드                 │   도구 정비 트랙
│   - NavMesh 편집 (Picking/스냅/Winding)           │
│   - Scene I/O 포맷 v1 (Map + NavMesh + Player)    │
└────────────────────────────────────────────────────┘
   │
   ▼
┌────────────────────────────────────────────────────┐
│ 트랙 3: Font + 2D UI 에디터                       │   "게임 같다" 트랙
│   - Logo / Loading 레벨 (이미지 + 텍스트)         │
│   - HUD 자리 (HP/스태미나/콤보 카운터)            │
└────────────────────────────────────────────────────┘
   │
   ▼
┌────────────────────────────────────────────────────┐
│ 트랙 4: 충돌 / Bounding / DebugDraw               │   전투 인프라 트랙
│   - Collider/Bounding (AABB/OBB/Sphere)           │
│   - DebugDraw (PrimitiveBatch)                    │
│   - 공격 히트박스 ↔ AnimNotify ATTACK_HIT 연결    │
└────────────────────────────────────────────────────┘
   │
   ▼
┌────────────────────────────────────────────────────┐
│ 트랙 5: Monster + 잔여 Player                     │   전투 성립 트랙
│   - 단일 몬스터 + AI 상태머신                     │
│   - LINK_ATTACK / CHAIN_SMASH / Step D / 블렌딩   │
└────────────────────────────────────────────────────┘
```

### 2.1 트랙 간 의존성 (왜 이 순서인가)

| 트랙 | 선행 트랙 | 이유 |
|------|----------|------|
| 1 카메라 | 없음 | Player 1차의 시각적 마감. 다른 트랙 작업이 어색한 카메라 위에서 진행되지 않게 |
| 2 NavMesh + Scene I/O | 1 | NavMesh 는 Monster (트랙 5) 의 직속 선행. Scene I/O 는 트랙 누적 시 절실해짐. 둘이 같은 직렬화 인프라를 공유 |
| 3 Font + UI | 2 | Logo/Loading/HUD 가 들어와야 "게임답게" 보임. 단계 2 가 직렬화 인프라 깔아두면 UI Scene 도 같은 패턴 재사용 |
| 4 충돌 | 3 | 트랙 5 (Monster) 의 직속 선행. Player 의 AnimNotify ATTACK_HIT 가 이미 박혀 있어 자연스럽게 연결 |
| 5 Monster + 잔여 Player | 1~4 | NavMesh + 충돌 + (HUD) 가 갖춰진 후에야 의미 있는 적이 됨. Player 잔여(LINK/CHAIN/D/블렌딩) 도 적이 있어야 검증 가능 |

### 2.2 트랙 진입 전 공통 체크

각 트랙 진입 직전:
1. 본 문서 해당 §의 **개념/대안/적용** 섹션 함께 읽기 (학습)
2. **Step 분해 초안** 을 보고 빠진 디테일 / 결정 미결 항목 합의
3. 합의 결과를 **Decision Log** (해당 § 하단) 에 추가
4. TaskCreate 로 Step 단위 트래킹 시작

---

## 3. 트랙 1 — 추적 카메라 마감 + Style C 이동 정책

### 3.1 개념: 추적 카메라란?

캐릭터를 따라가며 화면 중앙 근처에 유지하는 카메라. **자유 카메라** (Camera_Free, 현재 Editor용) 와 달리 입력이 카메라가 아니라 **캐릭터** 를 움직이고, 카메라는 결과를 따라간다.

핵심 변수 4개:
- **Target**: 따라갈 대상 (Player Transform)
- **Offset**: 타겟 기준 카메라 위치 (등 뒤 + 상단)
- **LookAt**: 카메라가 보는 지점 (보통 타겟 머리 부근)
- **Yaw / Pitch**: 마우스로 회전하는 카메라 자체의 자세

### 3.2 비교 대안

| 방식 | 설명 | 장점 | 단점 |
|------|------|------|------|
| **Direct Follow** | `CamPos = TargetPos + Offset(R(yaw))` 직접 계산 | 단순, 즉각 반응 | 끊김 — 빠른 회전 시 어지러움 |
| **Spring Arm (Lerp)** | 목표 위치를 매 프레임 lerp 추종 | 부드러움, 충돌 회피 가능 | 튜닝 필요 (Stiffness/Damping) |
| **Boom (Unreal)** | 타겟에서 뻗은 막대 끝 카메라 + 충돌 시 줄어듦 | 벽 뒤로 카메라 안 들어감 | 충돌 트레이스 비용. 본 프로젝트는 트랙 4 후에야 가능 |
| **Cinemachine 식 (Unity)** | 데드존 + 락온 + 우선순위 | 영화적 표현 | 본 프로젝트 규모엔 과함 |

**본 프로젝트**: **Spring Arm 즉시 추적 + 충돌 복귀용 Distance Lerp** 로 시작. 평상시에는 타겟 기준점과 카메라 거리를 즉시 유지해 액션 조작감을 안정화하고, 트랙 4 이후 벽 충돌로 arm 길이가 줄어든 경우에만 원래 거리 복귀를 Lerp 처리한다. Target/Pivot smoothing 은 이동 중 고무줄 느낌을 만들 수 있어 트랙 1 범위에서 제외한다.

### 3.3 Style C 이동 정책 (계승, v2 §4.1 결정)

이미 결정된 사항:
- WASD 는 **카메라 Yaw 기준 월드 방향벡터** 로 해석
- Player 는 목표 방향으로 **회전 속도 한계 내 보간 회전** + 동시 전진
- 비락온 한정 **스트레이프 없음** (A/D 는 좌우 측면이 아니라 "왼쪽/오른쪽으로 달리며 회전")
- Mouse X → 카메라 Yaw (Player 아님)

현재 코드 기준 필요한 것: **기존 Camera_Follow/SpringArm 마감 + Intent 구조 정리 + Player 삭제/재생성 시 target 재바인딩**.

### 3.4 본 프로젝트 적용

```
[입력]  Mouse X Δ ──→ Camera_Follow.Yaw 직접 갱신
        WASD       ──→ IntentResolver: Camera Yaw 기준 월드 방향 산출
                       → PLAYER_INTENT_FRAME::vMoveDirWorld (정규화)

[Player] Apply_MoveIntent(vMoveDirWorld):
         목표 Yaw = atan2(vMoveDirWorld.x, vMoveDirWorld.z)
         현재 Yaw 와 차이를 m_fRotationPerSec 한계 내 보간
         회전 후 Position += vMoveDirWorld * fSpeed * dt  (단순 직진 합성)

[Camera_Follow] Update:
         TargetWorldPos = Layer_Player 의 Player Transform 자동 검색/재바인딩
         TargetPoint = TargetWorldPos + HeightOffset
         LookDir = Yaw/Pitch 로 계산
         Eye = TargetPoint - LookDir * CurrentDistance
         CurrentDistance 는 평상시 IdealDistance. 트랙 4 충돌 시 줄어든 거리에서 복귀할 때만 Lerp
```

**핵심 결정**:
- `Camera_Free` 프로토타입은 Client Loader 에 유지. Editor 가 Client 런타임 리소스를 호스팅하므로 제거하지 않는다.
- GamePlay 런타임 인스턴스는 `Camera_Follow` 만 생성. `Camera_Free` 인스턴스는 Editor `Bootstrap_EditScene()` 에서만 생성한다.
- `Camera_Follow` 는 `Layer_Player` 기준 자동 검색/재바인딩으로 Player 삭제/재생성 시 dangling pointer 를 피한다.
- Intent 필드 교체: `vMoveAxis` / `bHasMoveIntent` 제거 → `vMoveDirWorld` 를 최종 이동 의미의 SSOT 로 사용. 이동 여부 판단은 `Client_Func.h::Has_MoveIntent()` 로 통일한다.

### 3.5 Step 분해 초안

| Step | 작업 | 검증 |
|------|------|------|
| 1-A ✅ | 기존 `Camera_Follow` / `SpringArm` 구조 마감 (Yaw/Pitch, 즉시 추적, 충돌 복귀용 Distance Lerp 확장점) | 빌드 및 실행 확인. 이동 중 카메라 고무줄 느낌 제거 |
| 1-B ✅ | `PLAYER_INTENT_FRAME` 구조 정리 + `IntentResolver` 카메라 Yaw 기준 방향 산출 | `vMoveAxis` / `bHasMoveIntent` 제거, `vMoveDirWorld` + `Has_MoveIntent()` 로 통일 |
| 1-C ✅ | `CPlayer::Apply_MoveIntent` Style C 유지/정리 (보간 회전 + 직진) | 빌드 및 실행 확인. 카메라 Yaw 기준 이동 유지 |
| 1-D ✅ | `Camera_Free` 역할 정리 | 프로토타입은 Client Loader 유지. GamePlay 는 Follow 만 생성, Editor Bootstrap 에서만 Free 생성 |
| 1-E ✅ | `Camera_Follow` target dangling 가드 | `Layer_Player` 자동 검색/재바인딩. Player 삭제/재생성 시 크래시 방지 방향 적용 |
| 1-F | 트랙 1 커밋 | `Track 1: 추적 카메라 + Style C 이동` |

### 3.6 미결 / 학습 포인트

- **Pitch 처리**: Mouse Y 기반 Pitch + `PitchMin/PitchMax` clamp 를 유지하기로 결정. 락온 카메라 도입 시 재검토.
- **Smoothing 정책**: 평상시 Target/Pivot smoothing 은 제외. 액션 조작감과 거리 안정성을 우선한다.
- **충돌 처리**: 트랙 4 충돌 시스템 도입 후 Boom 식 트레이스 추가. 벽 충돌로 줄어든 arm 길이가 원래 거리로 복귀할 때만 Distance Lerp 사용.
- **(학습)** 행렬 합성: `RotY(yaw) * Offset` 의 좌/우 손좌표계 주의. DirectX 행우선 + LH.

### 3.7 Decision Log (트랙 진입 시 채움)

#### 2026-05-06 — 트랙 1 현재 구현 기준 재정렬
- **결정**: 트랙 1은 신규 구현이 아니라 기존 `Camera_Follow` / `SpringArm` / `vMoveDirWorld` / Style C 이동의 마감 및 구조 정리로 진행한다.
- **이유**: 현재 코드에 추적 카메라와 카메라 Yaw 기준 이동이 이미 들어와 있으므로, 계획서의 신규 생성 항목을 그대로 수행하면 중복 작업이 된다.
- **반영 위치**: `Client/Private/Camera_Follow.cpp`, `Engine/Private/SpringArm.cpp`, `Client/Private/IntentResolver.cpp`, `Client/Private/Player.cpp`, `Client/Private/Player_StateMachine.cpp`

#### 2026-05-06 — Pitch 처리
- **결정**: Mouse Y 기반 Pitch 입력과 `PitchMin/PitchMax` clamp 를 유지한다.
- **이유**: 이미 `SpringArm` 에 구현되어 있고, 추후 락온 카메라에서도 pitch 제한은 필요하다. 지금 제거하면 나중에 다시 복구할 가능성이 높다.
- **후속 액션**: 락온 카메라 도입 시 Pitch 범위와 입력 정책을 재검토한다.

#### 2026-05-06 — SpringArm smoothing 정책
- **결정**: 평상시에는 Player target point 와 카메라 거리를 즉시 추적한다. Target/Pivot smoothing 은 트랙 1 범위에서 제외한다.
- **이유**: Player 이동 중 Target/Pivot smoothing 이 들어가면 카메라가 고무줄처럼 늘어지는 느낌이 생기고, 빠른 액션 조작감과 화면 중심 안정성을 해친다.
- **결정**: Distance Lerp 는 트랙 4 카메라 충돌 처리 이후, 벽 충돌로 줄어든 arm 길이가 `IdealDistance` 로 복귀할 때만 사용한다.
- **반영 위치**: `Engine/Private/SpringArm.cpp::Update_Arm`

#### 2026-05-06 — Camera target 재바인딩 정책
- **결정**: 트랙 1에서는 `Camera_Follow` 가 `Layer_Player` 를 기준으로 Player Transform 을 자동 검색/재바인딩한다. Player 가 없으면 target 을 `nullptr` 로 두고 마지막 카메라 상태 또는 기본 target point 를 사용한다.
- **이유**: 현재는 안정적인 SceneObjectId/GUID 가 없으므로 포인터를 저장해 장기 참조하는 방식은 dangling 위험이 있다. Layer 기반 자동 검색은 현재 구조에서 수정 범위가 작고, Player 삭제 후 재생성에도 대응 가능하다.
- **후속 액션**: 트랙 2 Scene I/O 에서 `SceneObjectId/GUID` 와 Object Lifetime Event 를 설계해, 장기적으로는 `TargetObjectId -> Transform` 재바인딩 구조로 전환한다.

#### 2026-05-06 — Camera_Free 유지 범위
- **결정**: `Prototype_GameObject_Camera_Free` 는 Client Loader 에 유지한다. `GamePlay` 는 `Ready_Layer_Camera` 에서 `Camera_Follow` 만 생성하고, `Camera_Free` 인스턴스는 Editor 의 `Bootstrap_EditScene()` 에서만 생성한다.
- **이유**: Editor 가 Client 런타임 리소스를 호스팅하므로 프로토타입 제거는 Editor 회귀를 만든다. 현재 구조는 플레이 런타임과 에디터 카메라 역할을 이미 분리하고 있다.
- **검증**: `CLevel_GamePlay::Ready_Layer_Camera` 는 `Camera_Follow` 생성만 수행하고, `CEditorApp::Bootstrap_EditScene` 에서만 `Camera_Free` 생성이 확인됨.

#### 2026-05-06 — Player Intent 이동 기준
- **결정**: 최종 이동 의미의 SSOT 는 `PLAYER_INTENT_FRAME::vMoveDirWorld` 로 둔다. `vMoveAxis` 와 `bHasMoveIntent` 는 제거하고, 이동 여부 판단은 `Client_Func.h::Has_MoveIntent(const PLAYER_INTENT_FRAME&)` 로 통일한다.
- **이유**: `vMoveAxis` 는 입력 좌표계의 중간값이고, 최종 소비자인 Player/StateMachine 은 월드 이동 방향만 알면 된다. 이동 여부를 `vMoveDirWorld` 길이로 판단하면 Intent 필드 간 불일치 가능성을 줄일 수 있다.
- **유지 사항**: `CPlayer_StateMachine::m_bLastHasMoveIntent` 는 유지한다. 이 값은 Intent 중복 필드가 아니라 Dash 종료 Notify 이후 전이를 위한 StateMachine 내부 상태다.

#### 2026-05-06 — Editor 참조 바인딩 UX 방향
- **결정**: Inspector 에서 참조를 수동 바인딩할 때 Hierarchy drag-drop 이동과 충돌하지 않도록 `Pick Target` 모드를 우선 검토한다.
- **이유**: 현재 Hierarchy drag-drop 은 오브젝트 이동/재정렬 의미로 이미 사용 중이다. 같은 drag-drop 을 참조 바인딩에도 쓰면 의도 충돌이 생긴다.
- **후속 액션**: 트랙 2 Scene I/O 와 `SceneObjectId/GUID` 도입 후 Camera target, AI target, Socket target 등에 공통 적용 가능한 참조 선택 UX 로 확장한다.

#### 2026-05-06 — 검증 결과
- **결정**: 트랙 1 코드 변경은 사용자가 직접 적용했고, Debug 빌드 및 실행 확인을 완료했다.
- **검증 내용**: `SpringArm` 즉시 추적 유지, 충돌 복귀용 Distance Lerp 구조, `Camera_Follow` 자동 재바인딩, `PLAYER_INTENT_FRAME` 구조 정리 적용 후 실행 확인.
- **Editor 검증**: Editor 에서 Player 삭제 시 `Camera_Follow` 는 마지막 위치를 유지하며 크래시 없이 동작하고, Player 재생성 시 `Layer_Player` 자동 검색으로 Follow 추적을 재개함.

---

## 4. 트랙 2 — NavMesh + Scene 저장/로드

### 4.1 개념: NavMesh 란?

**Nav**igation **Mesh**. 캐릭터(보통 AI) 가 이동할 수 있는 영역을 **삼각형 셀들의 집합** 으로 표현. 각 셀은 **인접 정보** (어느 변에 어떤 셀이 있는지) 를 들고 있어 그래프 탐색이 가능하다.

핵심 사용처:
1. **이동 제약**: 셀 밖으로 안 나가게 (벽/낭떠러지)
2. **경로 탐색**: 시작 셀 → 목표 셀까지의 셀 시퀀스 (A*)
3. **AI 추적**: Monster 가 Player 셀까지 경로 계산해 추적

### 4.2 비교 대안

| 방식 | 설명 | 장점 | 단점 |
|------|------|------|------|
| **자동 생성 (Recast/Detour)** | 메쉬에서 voxel-bake 로 NavMesh 자동 산출 | 손 댈 필요 없음. 산업 표준 (Unity 내장, Unreal NavSystem) | 라이브러리 의존, 본 학습 목적과 거리 멈 |
| **수동 셀 편집 (수업 코드, 본 프로젝트)** | Picking 으로 정점 찍어 셀 직접 만듦 | 학습용으로 좋음, 의존 없음, 제어 완전 | 소규모 맵에만 현실적, 정밀도 사용자 책임 |
| **그리드/노드** | 격자 셀 ON/OFF + 4/8 방향 인접 | 단순 | 자유 형태 표현 어려움, 대각선 비용 부정확 |
| **Voronoi / 시각적 그래프** | 장애물 분포에서 점들 산출 | 좁은 통로 표현 좋음 | 구현 복잡 |

**본 프로젝트**: **수동 셀 편집** + Picking + 정점 스냅 + Winding 자동 보정. 수업 코드 9개월차 2~5일차 패턴 계승. 학습 목적에 가장 부합.

### 4.3 본 프로젝트 적용 — 편집 워크플로우

수업 코드 핵심 클래스:
- `CCell` (`9개월차/4일차/Engine/Public/Cell.h:7`) — 정점 3개, 법선 3개, 인접 인덱스 3개, 평면 1개
- `CNavigation` (`...Navigation.h:7`) — `vector<CCell*>` + 현재 셀 인덱스 + 부모 매트릭스
- `CVIBuffer_Cell` (3일차+) — 셀 가시화용 와이어프레임
- A* (5일차/AStar 폴더) — `Make_Route(iGoalIndex)` → `list<_vector>`

**우리가 추가할 것 (수업 코드와 다른 점)**:
- Editor Picking 기반 정점 입력 (수업 코드는 외부 도구로 만든 데이터 파일 fread)
- 정점 스냅 (월드 거리 임계값 기준 기존 정점 재사용)
- Winding 자동 보정 (Cross.y 부호로 CCW 통일)
- NavMesh 데이터 별도 파일 저장 (`.navdata` 형식 — 또는 Scene I/O 와 통합)

**편집 흐름 (확인된 사양)**:
1. Editor "NavMesh Edit Mode" 토글
2. 맵 메쉬에 raycast → 표면 좌표 채택
3. 임계 거리 안에 기존 정점 있으면 **기존 정점 ID 재사용**, 없으면 **신규 정점 추가**
4. 같은 셀 안에서 동일 정점 ID 가 중복되면 생성 거부
5. 정점 3개가 모이면 삼각형 면적 검사 후 **자동 셀 생성** (Winding 보정 후)
6. Cell 생성/삭제/정점 이동 후 Neighbor 전체 재계산
7. 입력 미완(1·2번째)일 때 미리보기 + ESC 취소
8. Cell 선택 / 삭제 / 단일 정점 드래그 (인접 셀 영향)
9. 데이터 변경 명령에 대한 Undo/Redo
10. 저장 (`.navdata`), 다음 부팅 시 로드

### 4.4 Scene 저장/로드 (트랙 2 후반부)

NavMesh 가 첫 직렬화 대상이 되는 김에 **Scene I/O 인프라 v1** 도 같이 깐다.

**v1 저장 대상**:
- 맵 오브젝트 배치 (prototype tag, layer, position/rotation/scale, parts/socket)
- NavMesh 데이터 (정점 + 셀 인접)
- Player 스폰 포인트 (PLAYER_DESC 일부)

**향후 확장 (v2~)**: Monster 스폰, 이펙트, 라이트, UI Scene.

**포맷 결정 (계승)**: JSON(편집/확인 가능) + 바이너리(런타임). 처음부터 **version 필드 + 누락 필드 폴백** 패턴 박아두면 SLMD 처럼 v1→v2 비파괴 마이그레이션 가능.

### 4.5 Step 분해 초안

| Step | 작업 | 검증 |
|------|------|------|
| 2-A | `CCell` / `CNavigation` Engine 이식 (수업 코드 4일차 기준) | 미완성 상태 빌드 통과 |
| 2-B | `CVIBuffer_Cell` 와이어프레임 가시화 | DebugDraw 없이도 셀 보임 |
| 2-C | Editor "NavMesh Edit Mode" 토글 + Picking → 정점 채택 | 마우스 클릭 위치에 점 표시 |
| 2-D | 정점 스냅 (월드 거리 임계값) + 미리보기 | 두 번째 셀 시작 시 인접 셀 정점 자동 재사용 |
| 2-E | 3정점 모이면 자동 셀 생성 + Winding 보정 | 어느 순서로 찍어도 같은 방향 셀 |
| 2-F | Cell 선택 / 삭제 / 단일 정점 이동 / Undo·Redo | 삭제 Undo 복원, 인접 셀 같이 변형 |
| 2-G | NavMesh 데이터 저장/로드 (v1 포맷, version 분기) | 다시 켜도 동일하게 로드 |
| 2-H | A* 이식 (5일차 AStar 패턴) | Player 임의 두 셀 사이 경로 계산 (가시화) |
| 2-I | Scene I/O 포맷 v1 (Map 배치 + NavMesh 통합) | Editor 저장 → GameApp 로드 동일 결과 |
| 2-J | 트랙 2 커밋 | `Track 2: NavMesh + Scene I/O v1` |

### 4.6 미결 / 학습 포인트

- **NavMesh 와 맵 충돌 분리**: NavMesh 는 "어디까지 갈 수 있나" (큰 영역), 트랙 4 의 충돌은 "이 점이 닿았나" (정밀). 둘은 별개 시스템 — 혼동 금지.
- **Player 도 NavMesh 제약?**: 본 프로젝트에서는 Player 도 NavMesh 위를 이동한다. Track 2 에서 Player 이동을 NavMesh 로 먼저 검증하고, Track 5 Monster/AI 가 같은 구조를 재사용한다.
- **셀 정점 공유 vs 비공유**: 공유 시 인접 그래프 자동, 한 정점 수정이 여러 셀 영향. 비공유 시 인접은 좌표 비교 필수, 수정 영향 격리. 수업 코드는 공유 — **본 프로젝트도 공유** 권장.
- **A* 휴리스틱**: 셀 중심 간 직선거리 (Manhattan/Euclidean). 본 프로젝트는 Euclidean 충분.
- **Scene I/O 포맷 versioning**: 처음부터 magic + version 박을 것. 누락 필드는 빈 값 폴백 (SLMD 패턴). 학습 포인트.
- **(학습)** Cross product 의 Y 부호로 winding 판정하는 법: `(B-A) × (C-A)`, .y > 0 이면 CCW (위에서 봤을 때).

### 4.7 Decision Log

#### 2026-05-06 — NavMesh 소유권 / Agent 컴포넌트 구조
- **결정**: NavMesh 원본 데이터는 Scene/Level 쪽에서 하나만 소유한다. Player, Monster 처럼 길찾기 또는 이동 제약이 필요한 객체는 `NavigationAgent` 컴포넌트를 소유한다.
- **구조**: `Player/Monster -> CNavigationAgent` 는 기존 컴포넌트 패턴처럼 `Add_Component` / `Safe_Release` 대상이다. `CNavigationAgent -> CNavMesh` 는 비소유 참조이며, Agent 는 현재 Cell Index / 경로 상태 / 이동 가능 검사 상태만 가진다.
- **이유**: NavMesh 는 캐릭터의 PartObject 가 아니라 맵 이동 데이터다. 데이터는 하나만 두고, 객체별 상태만 Agent 에 분리해야 Editor 저작, Player 검증, Monster 확장이 모두 자연스럽다.
- **초기 바인딩**: 현재 `CGameInstance::Add_GameObject()` 는 생성 객체 포인터를 반환하지 않으므로, 1차 구현은 Agent 가 `Layer_NavMesh` 또는 Scene NavMesh 서비스를 조회해 런타임 바인딩하는 방식으로 시작한다.

#### 2026-05-06 — Player NavMesh 적용 범위
- **결정**: Track 2 에서 Player 도 NavMesh 위를 이동하도록 한다.
- **이유**: Player 이동을 먼저 NavMesh 로 제한하면 Editor 저작 결과를 즉시 검증할 수 있고, Track 5 Monster/AI 가 같은 NavigationAgent 구조를 재사용할 수 있다.
- **1차 이동 정책**: 이동 후보 위치가 현재 Cell 또는 Neighbor Cell 안이면 이동 허용 + 높이 보정. 막힌 Edge 이면 이동 차단. Sliding 은 Track 2 후반 또는 충돌 Track 이후 재검토한다.

#### 2026-05-06 — Cell 저작 데이터 구조
- **결정**: Editor NavMesh 는 공유 정점 풀 기반으로 관리한다. Cell 은 좌표 3개를 직접 들기보다 `VertexIndex[3]` 를 들고, Neighbor 는 `NeighborIndex[3]` 로 관리한다.
- **정점 스냅**: Picking 좌표가 기존 정점의 Snap Radius 안에 있으면 좌표를 복사하는 것이 아니라 기존 정점 ID 를 재사용한다.
- **Winding 보정**: 3정점이 모이면 `(B-A) x (C-A)` 의 Y 부호로 방향을 판정하고, 반대 방향이면 두 정점 인덱스를 swap 해 Cell winding 을 통일한다.
- **생성 방어 조건**: 동일 Cell 내 중복 정점 ID 금지, 면적이 너무 작은 삼각형 금지, Cell 생성/삭제/정점 이동 후 Neighbor 전체 재계산.
- **이유**: 공유 정점 기반이어야 정점 드래그 시 인접 Cell 이 함께 변형되고, Neighbor 계산도 "같은 정점 ID 2개 공유" 기준으로 안정화된다.

#### 2026-05-06 — Cell 선택 / 삭제 / Undo
- **결정**: Track 2 Editor 범위에 Cell 선택, Cell 삭제, Undo/Redo 를 필수 기능으로 포함한다.
- **Undo 대상**: Cell 생성, Cell 삭제, 정점 이동, NavMesh Clear 같은 데이터 변경 작업.
- **Undo 비대상**: Hover, Cell 선택, 생성 중인 1~2개 Preview 정점은 1차 구현에서 Undo 대상에서 제외한다.
- **초기 구현**: Undo/Redo 는 Command delta 방식이 아니라 NavMesh 전체 Snapshot 방식으로 시작한다. NavMesh 규모가 작고, 삭제/정점 이동/Neighbor 재계산의 복원 안정성이 더 중요하기 때문이다.
- **후속 액션**: NavMesh 규모가 커지거나 편집 명령이 복잡해지면 Snapshot 방식에서 command delta 방식으로 최적화한다.

---

## 5. 트랙 3 — Font + 2D UI 에디터

### 5.1 개념

게임 화면에 **텍스트** (HP 숫자, 메뉴, 데미지 폰트) 와 **2D 이미지** (UI 패널, 아이콘, 로고) 를 그리는 인프라.

3D 캐릭터는 World 공간에서 그려지지만 UI 는 **Screen 공간** (혹은 Orthographic projection) 에서 그린다. 깊이/조명 영향 없이 픽셀 단위 정확하게 배치.

### 5.2 비교 대안

| 방식 | 설명 | 장점 | 단점 |
|------|------|------|------|
| **DirectXTK SpriteFont** (수업 코드, 본 프로젝트) | `.spritefont` 파일을 makespritefont 도구로 미리 생성, SpriteBatch 로 그림 | 의존 가벼움, 즉시 동작 | 한글 폰트 별도 생성 필요, 동적 글리프 불가 |
| **FreeType + Atlas** | TTF 파일에서 동적으로 글리프 래스터화 | 어떤 폰트든, 어떤 크기든 | 구현 복잡, 학습 비용 큼 |
| **Bitmap Font** | 미리 그린 PNG + 좌표 테이블 | 단순, 게임 룩에 맞춤 | 크기 변경 어려움 |
| **ImGui** | (이미 Editor 에 있음) | Editor 패널로 충분 | 게임 런타임 UI 에는 부적합 |

**본 프로젝트**: **DirectXTK SpriteFont** 시작. 한글 게임이라 spritefont 도구 사용법 학습 필요. 후속에 FreeType 으로 갈지는 트랙 마감 후 판단.

### 5.3 본 프로젝트 적용

수업 코드 1일차 핵심 클래스:
- `CFont_Manager` — 싱글톤 래퍼, `Add_Font(tag, path)` / `Render(tag, text, ...)`
- `CCustomFont` — DirectXTK `SpriteBatch` + `SpriteFont` 캡슐
- `Engine_Defines.h` 에 `#include <SpriteBatch.h> <SpriteFont.h>` 추가

**2D UI 오브젝트**: 이미 본 프로젝트에 `CUIObject` / `UIOBJECT_DESC` 기반은 있음. 트랙 3 에서 활성화 + Editor 배치 도구.

**결과물**:
1. **Logo Level**: 회사 로고 + "Press Any Key" 텍스트 (5초 후 또는 키 입력 시 Loading 이동)
2. **Loading Level**: 진행 막대 + "Loading..." 텍스트 + 배경 이미지 (리소스 비동기 로드 후 GamePlay 이동)
3. **HUD 자리**: GamePlay 레벨 위에 HP/스태미나/콤보 카운터 자리만 잡기 (값은 트랙 5 Monster + Player 시점에 채움)

### 5.4 Step 분해 초안

| Step | 작업 | 검증 |
|------|------|------|
| 3-A | DirectXTK SpriteBatch/SpriteFont Engine 의존 추가 | 빌드 통과 |
| 3-B | `CFont_Manager` + `CCustomFont` 이식 + `CGameInstance` 위임 | 임의 좌표에 텍스트 |
| 3-C | spritefont 한글 폰트 생성 (`makespritefont` 도구 사용법) | 한글 텍스트 렌더 |
| 3-D | `CUIObject` 활성화 + `UIOBJECT_DESC` 사용 사례 1개 | 화면에 이미지 한 장 |
| 3-E | `CLevel_Logo` (회사 로고 + Press Any Key) | 부팅 시 로고 보임 |
| 3-F | `CLevel_Loading` (진행 막대 + 비동기 로드) | Logo→Loading→GamePlay 흐름 |
| 3-G | HUD 자리 잡기 (HP/스태미나/콤보 카운터 빈 슬롯) | 값 0 으로 그려짐 |
| 3-H | (선택) Editor `CPanel_2DCanvas` — UI 배치 도구 | UI 위치 드래그 가능 |
| 3-I | 트랙 3 커밋 | `Track 3: Font + 2D UI + Logo/Loading/HUD 자리` |

### 5.5 미결 / 학습 포인트

- **레벨 전환 흐름**: 현재는 `CLevel_GamePlay` 만 있음. Logo → Loading → GamePlay 흐름은 `CLevel_Manager::Open_Level` 로 처리. 비동기 로드는 `CLoader` 스레드 (이미 있음). `CoInitializeEx(nullptr, 0)` 필수 (CLAUDE.md Gotchas).
- **UI Scene vs 3D Scene 분리**: 3D Scene 과 UI Scene 을 별도 파일로 관리할지, 한 Scene 에 합칠지. 산업 표준은 분리 (Unity prefab vs UI Canvas). 트랙 2 Scene I/O 와 일관성 검토.
- **HUD 데이터 바인딩**: HUD 가 Player 의 HP 를 어떻게 읽나? 직접 포인터 vs Pub/Sub vs 폴링. 본 프로젝트는 폴링이 SSOT 와 잘 어울림 (Inspector 와 같은 패턴).
- **Logo/Loading 이미지 에셋**: 트랙 3 진입 전 사용자가 준비. (아니면 임시 placeholder 로 시작)
- **(학습)** Orthographic projection: World 좌표 (-1, +1) 또는 (0, screen_size) 직접 매핑.

### 5.6 Decision Log

*(빈 공간)*

---

## 6. 트랙 4 — 충돌 / Bounding / DebugDraw

### 6.1 개념

**Bounding** = 물체를 감싸는 단순 도형 (Sphere/AABB/OBB). 정밀한 메쉬-메쉬 충돌은 비싸므로 **단순 도형끼리 먼저** 검사.

**Collider** = Bounding 을 들고 있는 컴포넌트. 매 프레임 Transform 으로 업데이트.

**DebugDraw** = 충돌 형태/광선/그리드 등을 와이어프레임으로 시각화 (DirectXTK `PrimitiveBatch` + `BasicEffect`).

### 6.2 비교 대안

| 방식 | 설명 | 장점 | 단점 |
|------|------|------|------|
| **PhysX / Bullet / Jolt** | 산업 표준 물리 엔진 | 물리 시뮬레이션, 안정성 | 본 학습 목적과 거리 멈, 의존 무거움 |
| **DirectXTK BoundingSphere/Box/OrientedBox** (수업 코드, 본 프로젝트) | DirectXMath 기본 도형 + Intersects 함수 | 의존 가벼움, 학습용 | 정밀 충돌 / 물리 응답 없음 |
| **수동 GJK/SAT** | OBB-OBB 등 직접 구현 | 학습 깊이 | 구현 비용 큼 |

**본 프로젝트**: **DirectXTK Bounding** 시작. PrimitiveBatch 로 와이어프레임. 수업 코드 6일차 패턴.

### 6.3 본 프로젝트 적용

수업 코드 6일차 핵심:
- `CBounding` (abstract, `9개월차/6일차/Engine/Public/Bounding.h:7`) — Update / Render(`PrimitiveBatch`)
- `CBounding_AABB / OBB / Sphere` — 각 도형
- `CCollider` (`Collider.h:10`) — `COLLIDER eType` + `m_pBounding` + `PrimitiveBatch` + `BasicEffect`
- `DebugDraw` (`DebugDraw.h`) — DirectXTK 헬퍼 (Microsoft MIT 라이선스)

**우리 적용 포인트**:
1. **공격 히트박스 ↔ AnimNotify ATTACK_HIT**: 이미 콤보 단계별 OPEN/CLOSE Notify 박혀 있음 (`Step C-6`). 트랙 4 에서 Collider on/off 를 Notify 콜백으로 연결.
2. **피격 판정**: 캐릭터 바디 콜라이더 (Sphere or Capsule 근사). Player vs 공격 / Monster vs 공격.
3. **맵 충돌**: 정적 메쉬 콜라이더는 후순위 (NavMesh 가 이미 큰 영역 제약 담당).
4. **DebugDraw**: Editor Viewport 디버그 모드 토글 (콜라이더 / NavMesh / Ray 가시화). 트랙 2 에서 NavMesh 자체 가시화는 `VIBuffer_Cell` 로 했으므로 DebugDraw 는 콜라이더용.

### 6.4 Step 분해 초안

| Step | 작업 | 검증 |
|------|------|------|
| 4-A | DirectXTK PrimitiveBatch/BasicEffect 의존 추가 | 빌드 통과 |
| 4-B | `CBounding` + `_AABB/_OBB/_Sphere` 이식 | (가시화 없이) Update 동작 |
| 4-C | `CCollider` 컴포넌트 (Component 파생, 양방향 Clone) | 다른 게임오브젝트에 부착 |
| 4-D | `DebugDraw.h/cpp` 통합 + Editor 디버그 모드 토글 | 와이어프레임 보임 |
| 4-E | 충돌 판정 함수 (`Intersects` 래퍼) | Player Sphere ↔ 정지 Box 검출 |
| 4-F | 공격 히트박스 인프라 (BasicAttack 단계별 ON/OFF) | ATTACK_HIT Notify 발화 시 잠깐 ON |
| 4-G | 피격 판정 — Player 자체 Body Collider | 공격이 Player 에 닿는지 판정 (실제 데미지 없이 로그) |
| 4-H | 트랙 4 커밋 | `Track 4: Collider + Bounding + DebugDraw + 히트박스` |

### 6.5 미결 / 학습 포인트

- **Body Collider 형태**: Sphere (가장 단순), Capsule (사람 형태), AABB (대충), OBB (회전 정확). 사람 캐릭터는 보통 **Capsule** 인데 DirectXTK 에 직접 없음 — Sphere 2개 + Cylinder 흉내내거나 그냥 Sphere 로 시작.
- **공격 히트박스 시간/공간**: 시간 = `COMBO_WINDOW_OPEN/CLOSE` 사이? 새 Notify (`ATTACK_HITBOX_ON/OFF`) 도입? 공간 = 무기 본 기준 OBB? 무기 메쉬 본 위치 + 정해진 오프셋?
- **Damage 적용 시점**: 트랙 4 에서는 "충돌 검출까지" 만. 실제 HP 차감은 트랙 5 Monster 도입 후 (Monster 가 받을 게 있어야 의미).
- **Broad/Narrow phase**: 본 프로젝트 규모에선 모든 콜라이더 N² 검사로 충분. 그리드/Octree 는 후순위.
- **(학습)** AABB vs OBB: AABB 는 회전 안 됨 (월드축 정렬). OBB 는 회전 가능 (캐릭터 회전 시 정확). Sphere 는 회전 무관 (가장 빠름, 가장 부정확).

### 6.6 Decision Log

*(빈 공간)*

---

## 7. 트랙 5 — Monster + 잔여 Player

### 7.1 개념

**적 캐릭터** + **AI 상태머신**. Player 의 `CPlayer_StateMachine` 과 같은 패턴이지만 입력 대신 **AI 결정** 이 다음 액션을 정한다.

세 가지 AI 패러다임:
| 방식 | 설명 | 장점 | 단점 |
|------|------|------|------|
| **FSM (Finite State Machine)** | 상태 + 전이. 본 프로젝트 Player 와 같은 패턴 | 단순, 디버깅 쉬움 | 상태 폭발, 복잡 행동 표현 어려움 |
| **Behavior Tree** | 트리 노드 (Sequence/Selector/Decorator) | 모듈화, 디자이너 친화 | 학습 비용, 본 프로젝트 규모엔 과함 |
| **Utility AI / GOAP** | 효용 함수로 행동 점수화 | 자연스러운 결정 | 튜닝 까다로움 |

**본 프로젝트**: **FSM** (Player 와 같은 패턴 재사용). 단일 몬스터 6~8 상태면 FSM 으로 충분.

### 7.2 본 프로젝트 적용

기본 상태 (수업 코드 / 일반 패턴):
- **Idle** — 대기, 주변 감지
- **Patrol** — 정해진 경로 순찰 (NavMesh 사용)
- **Chase** — Player 발견 시 추격 (NavMesh A*)
- **Attack** — 사거리 안 들어왔을 때
- **Hit** — 피격 반응
- **Stun** — 경직
- **Death** — 사망

**선행 인프라**:
- 트랙 2 NavMesh — Patrol/Chase 경로
- 트랙 4 Collider — 사거리 판정, 피격 검출
- AnimNotify (이미 있음) — 공격 히트박스 ON/OFF, 사망 처리

**클래스 구조**:
- `CMonster` (Container) → `CBody_Monster` (Anim Part) — Player 와 대칭
- `CMonster_StateMachine` — `CStateMachine` 파생, AI 결정 로직
- `MonsterAnimTable` (이미 v2 §1 에 정의)
- `MONSTER_ACTION + MONSTER_PHASE` 키 (이미 정의)

### 7.3 잔여 Player 작업 (트랙 5 후반부)

Player 1차 마감 시 보류한 항목들. 이제 적이 있으니 검증 가능:

| 항목 | 작업 | 의존 |
|------|------|------|
| **LINK_ATTACK / CHAIN_SMASH** | DASH+LMB → LINK_ATTACK, LINK_ATTACK + LMB(OPEN) → CHAIN_SMASH | Monster (검증 대상) |
| **Step D 무기 스왑** | `WEAPON_SWAP` + `CORE_ATTACK_01` + Cooldown + `CoreAttack_Trigger` Notify | 기존 인프라 |
| **단계 2-3 블렌딩 재설계** | Freeze A → Lerp → Play B 또는 전이 엣지 BlendTime 속성 | 기존 AnimController |

**순서 권장**: Monster 기본 골격 → 잔여 Player → Monster AI 정교화. Player 잔여를 너무 미루면 Monster 만 화려해지고 Player 가 단순해짐.

### 7.4 Step 분해 초안 (큰 골격만 — 진입 시 구체화)

| Step | 작업 | 비고 |
|------|------|------|
| 5-A | Monster `.bin` 확보 + MonsterAnimTable 구체화 | 사용자 에셋 의존 |
| 5-B | `CMonster` + `CBody_Monster` 클래스 구조 | Player 패턴 미러 |
| 5-C | `CMonster_StateMachine` + 기본 5 상태 (Idle/Patrol/Chase/Attack/Death) | NavMesh 사용 |
| 5-D | Player 공격 ↔ Monster 피격 (Collider 연결) | 트랙 4 결과 사용 |
| 5-E | Monster 공격 ↔ Player 피격 | HP 시스템 도입 |
| 5-F | HP/스태미나 값 HUD 연결 | 트랙 3 자리 채움 |
| 5-G | Player Step D 무기 스왑 + 쿨다운 | |
| 5-H | Player LINK_ATTACK / CHAIN_SMASH | |
| 5-I | (선택) 블렌딩 재설계 | 품질 축 |
| 5-J | 트랙 5 커밋 | |

### 7.5 미결 / 학습 포인트

- **단일 몬스터 vs 웨이브**: 단일부터. 웨이브/스폰은 후순위.
- **HP 시스템 위치**: Player/Monster 공통 부모? 독립 Component? — 공통 Stat Component 권장 (학습 가치).
- **AI 결정 주기**: 매 프레임 결정 vs N 프레임마다 (성능). 본 프로젝트 규모엔 매 프레임 OK.
- **Damage Pipeline**: Attacker → Hit Event → Defender → HP 차감 → Notify → 이펙트/사운드. 트랙 5 에서 단순 버전 (HP 차감 + 콘솔 로그). 이펙트는 후속.
- **AI 가시화**: 현재 상태 / 이동 경로를 DebugDraw 로 항상 그려두면 디버깅 천국.

### 7.6 Decision Log

*(빈 공간)*

---

## 8. Claude + Codex 공동 작업 가이드라인

### 8.1 두 도구의 특성 차이

| 항목 | Claude Code | Codex |
|------|-------------|-------|
| **컨텍스트** | 큰 메모리 + 세션 명세서 + 메모리 시스템 | 슬림한 컨텍스트, IDE 통합 강함 |
| **소스 직접 수정** | 본 프로젝트 정책상 금지 (인코딩 이슈) — 코드블록 제시 | 직접 파일 편집 가능 |
| **컴파일/빌드 검증** | 사용자가 수동 빌드 후 결과 보고 | Codex 가 직접 빌드 가능 |
| **계획/설계 토론** | 강함 (대화/맥락 유지) | 가능하지만 짧은 호흡 |
| **반복 자동화** | Agent 도구로 가능 | 강함 |

### 8.2 작업 분담 권장 패턴

| 작업 종류 | 주 담당 | 보조 |
|----------|---------|------|
| **명세서 작성/갱신** | Claude | — |
| **설계 / 트레이드오프 토론** | Claude | — |
| **단순 반복 패치 (네이밍 변경, 멤버 추가)** | Codex | Claude (검토) |
| **새 클래스 골격 작성** | Claude (코드블록 제시) → 사용자 / Codex (반영) | — |
| **빌드 에러 진단** | Codex (직접 빌드 결과 보고) | Claude (원인 추론) |
| **메모리/명세서 누적** | Claude | — |

### 8.3 동시 작업 충돌 방지

**원칙**: 같은 파일을 두 도구가 동시 수정하지 않음.

방법:
1. **트랙 단위 분담**: 한 트랙은 한 도구가 주도. (예: 트랙 1 Camera = Claude 가 코드블록 제시 + 사용자 반영, 트랙 2 NavMesh 수업 코드 이식 = Codex 가 직접 패치)
2. **Step 단위 핸드오프**: Step 끝날 때마다 git commit → 다른 도구가 pull
3. **명세서 갱신 시점 합의**: Step 종료 시점에만 본 문서 갱신. 진행 중 갱신 금지

### 8.4 핸드오프 체크리스트

도구 A → 도구 B 로 작업 넘길 때:

- [ ] 마지막 commit hash 와 메시지
- [ ] 현재 작업 중인 Step (어디까지 했는지)
- [ ] 빌드 통과 여부 + 마지막 런타임 검증 결과
- [ ] 미해결 디버그 로그 / TODO 위치 (file:line)
- [ ] 본 문서 해당 트랙 §의 Decision Log 최신 엔트리

핸드오프 메시지 형식 (예):
```
Track 2 Step 2-D 까지 완료, commit 7f3a2b1.
다음: 2-E (Winding 보정) — Cell.cpp:Initialize 에서 Cross.y 부호 검사 필요.
빌드 통과, Editor Picking 정점 스냅 임계값 0.1m 로 확인.
```

### 8.5 메모리 시스템 (Claude 측)

Claude 의 자동 메모리는 `C:\Users\chaho\.claude\projects\C--Users-chaho-ddoichaboom-Solo-Leveling\memory\` 에 영속.

- **user**: 사용자 정보
- **feedback**: 작업 방식 규약 (예: "소스 직접 수정 금지")
- **project**: 프로젝트 의사 결정 (예: CHARACTER_STATE 의미 분류)
- **reference**: 외부 시스템 위치 (예: 수업 코드 경로)

Codex 측은 자체 컨텍스트 / 사용자 입력 의존. 핸드오프 시 **Claude 의 프로젝트 메모리 핵심을 본 문서나 CLAUDE.md 에 옮겨두면** 두 도구 모두 일관된 결정을 따른다.

---

## 9. 명세서 갱신 규칙

### 9.1 문서 종류 / 책임

| 문서 | 책임 범위 | 갱신 주기 |
|------|----------|-----------|
| `CLAUDE.md` | 프로젝트 컨벤션, Gotchas, 영구 결정 | 영구 결정 발생 시 |
| **`통합_구현계획_v3.md` (본 문서)** | **트랙별 계획 + 결정 + 진행 상태 (단일 진실 출처)** | **트랙/Step 진입·종료 시** |
| `MM월 DD일 진행 사항.md` | 일자별 작업 로그 (디버깅 흐름 포함) | 작업 후 매일 |
| `수업 정리/N일차.md` | 수업 코드 학습 메모 | 학습 시 |
| `메모리 시스템` (Claude) | 작업 규약 + 미세 결정 | 자동 |

**원칙**: 본 문서 v3 가 트랙·결정의 SSOT. 다른 문서는 보조.

### 9.2 갱신 시점

| 시점 | 갱신 대상 |
|------|----------|
| **트랙 진입 직전** | §3~§7 해당 트랙 §의 미결 항목 합의 → Decision Log 추가 |
| **Step 시작** | Step 분해 표에 🔄 (진행 중) 마킹. TaskCreate |
| **Step 완료** | Step 분해 표에 ✅ + 한줄 결과. TaskUpdate completed |
| **트랙 완료** | §1.1 (완료 스냅샷) 으로 트랙 요약 이동. Decision Log 동결 표기 |
| **결정 변경** | 옛 결정에 ⚠️ + "deprecated YYYY-MM-DD: 새 결정 위치" + 새 결정 추가 |
| **새로운 학습/대안 발견** | 해당 트랙 §의 §X.2 (비교 대안) 에 행 추가 |

### 9.3 Decision Log 형식

각 트랙 §의 마지막 §X.7 (또는 §X.6) 에 누적:

```markdown
### N.7 Decision Log

#### YYYY-MM-DD — 안건 제목
- **결정**: 한 줄
- **이유**: 두~세 줄 (왜 이 옵션이 나은가, 무엇을 포기했는가)
- **반영 위치**: file:line 또는 본 문서 §X.Y
- **(필요 시) 후속 액션**: 다음에 검토할 것

#### YYYY-MM-DD — 다음 안건
...
```

### 9.4 스테일 처리

**삭제 금지** — 옛 결정은 ⚠️ 표기로 유지 (왜 바뀌었는지 추적 가능). 예:

```markdown
~~결정 (2026-04-21)~~ ⚠️ deprecated 2026-05-10
이유: § 3.7 Decision Log 2026-05-10 항목 참조
```

### 9.5 일자별 진행 사항 vs 본 문서

- **일자별** = 흐름 / 디버깅 / 시도와 실패 (서사적, 시간순)
- **본 문서** = 결과 / 결정 / 현재 상태 (구조적, 트랙순)

세션 끝나면 일자별 → 본 문서로 결정만 추출해 옮긴다.

---

## 10. 참고 자료 인덱스

### 10.1 수업 코드 9개월차 일자별 매핑

| 일차 | 추가된 핵심 | 본 프로젝트 트랙 |
|------|-----------|----------------|
| **1일차** | Font_Manager, CustomFont, Sky, VIBuffer_Cube, Logo/Loading 레벨 골격 | 트랙 3 |
| **2일차** | `CCell`, `CNavigation` (NavMesh v0) | 트랙 2 |
| **3일차** | + `CVIBuffer_Cell` (셀 가시화) | 트랙 2 |
| **4일차** | NavMesh 정제 (정점 3개, 인접 인덱스, 평면) | 트랙 2 |
| **5일차** | + `CCollider`, `CBounding`, `CBounding_AABB`, AStar 폴더 | 트랙 4, 트랙 2 (A*) |
| **6일차** | + `CBounding_OBB/Sphere`, `DebugDraw` | 트랙 4 |
| 7~10일차 | VIBuffer_Instance / Point_Instance (인스턴싱/파티클) | 후순위 (단계 9) |
| 14일차 | RenderTarget, Target_Manager (다중 RT) | 후순위 (단계 9) |

루트: `C:\Users\chaho\ddoichaboom\CPP_STUDY_3D\수업 파일\UnZips\9개월차\`

### 10.2 본 프로젝트 명세서 인덱스

| 분류 | 파일 |
|------|------|
| **트랙 계획 (본 문서)** | `통합_구현계획_v3.md` |
| 옛 통합 계획 | `통합_구현계획_v2.md`, `통합_구현계획.md` |
| Player 1차 세부 (잔여 §1.2 흡수됨) | `단계3_Player_세부계획.md` |
| Editor 전체 계획 | `Editor_전체_구현계획.md` |
| 일자별 진행 | `MM월 DD일 진행 사항.md` (4월 4일 ~ 5월 5일) |
| 수업 정리 (Assimp/Animation 계열) | `수업 정리/9~27일차 수업 정리.md` |

### 10.3 기타 참고 경로

| 자료 | 경로 |
|------|------|
| 4-Project DLL 원본 | `C:\Users\chaho\ddoichaboom\CPP_STUDY_3D\Framework_4Proj\Framework\CLAUDE.md` |
| 3-Project 원본 | `C:\Users\chaho\ddoichaboom\CPP_STUDY_3D\Framework\CLAUDE.md` |
| 수업 메모 9~14일차 (Assimp/Model/Animation 계열) | `C:\Users\denni\ddoichaboom\CPP_STUDY_3D\명세서\` |

---

## 11. 다음 액션

### 11.1 트랙 2 현재 합의 상태

트랙 1 주요 결정과 코드 적용/검증/커밋은 2026-05-06 완료. 현재 브랜치: `feature/NavMesh`.

트랙 2 진입 전 §4.7 에 반영된 현재 합의:

1. **Player 도 NavMesh 제약 대상** — Track 2 에서 Player 이동으로 먼저 검증하고, Track 5 Monster/AI 가 재사용
2. **NavMesh 소유권** — Scene/Level 이 NavMesh 원본 데이터를 소유, Player/Monster 는 `NavigationAgent` 컴포넌트 소유
3. **셀 정점 공유 방식** — 공유 정점 풀 기반. 편집 시 인접 Cell 동시 영향 허용
4. **Editor 필수 기능** — 정점 스냅, Winding 자동 보정, Cell 선택/삭제, Undo/Redo 포함
5. **아직 결정 필요** — NavMesh 저장 위치(`.navdata` 별도 우선 vs Scene I/O v1 통합), SceneObjectId/GUID 도입 범위, A* 이식 시점

### 11.2 진행 흐름

```
NavMesh 데이터 구조 v1 / 최소 인터페이스 확정
   ↓
TaskCreate: 트랙 2 Step 2-A ~ 2-J
   ↓
2-A: `CCell` / `CNavMesh` / `CNavigationAgent` Engine 설계 및 1차 이식
```

### 11.3 문서 갱신 약속

- 트랙 진행 중에도 **결정 변경** 발생 시 본 문서 즉시 갱신 (Decision Log 추가)
- Step 완료 시 ✅ + 결과 한 줄
- 이 약속이 지켜지지 않으면 명세서가 코드와 어긋나 재학습 비용 발생 → 우선순위 1순위

---

> **본 문서는 살아 있는 문서다.** 트랙별 결정이 누적될수록 가치가 커진다. 토론하다 합의된 것은 즉시 §X.7 Decision Log 에 박을 것.
