2026-03-27(금)

// Model.h
모델을 그려내기 위한 정점과 인덱스를 로드
vector로 여러개의 Mesh를 담아둠

// Mesh 클래스
// VIBuffer를 상속받는 자식 클래스로 선언
Assimp에서 던져주는 정점 정보대로 구조체를 만드는게 제일 좋음
정점 구조체 멤버를 64개를 전부다 할당해줌 -> 근데 이 중에 필요없는(유효하지않은) 것들도 포함되어 있음
위치 노말 텍스쿠드 탄젠트 바이노말
Position, Normal, Texcoord, Tangent, Binormal

mNumIncies 없고 -> mNumFaces

pAiMesh->m_Vertices : 정점들의 Position을 배열로 들고 있는 주소값

모델마다 사용하고 있는 TextureCoord의 개수가 다를 수 있으므로 더 필요하다면 늘릴 것.
텍스처 파일을 봤을 때 같은 그림에 색상이 다를 경우 하나의 Texcoord를 사용한다는 의미
모델 로드에 문제 생기면 Texturecoord 를 0 번째가 아닌 다른 거로 바꿔보기
m_Indicies : 인덱스 배열의 주소

FreeScene으로 Scene 삭제
모델을 바이너리화 해야함...
왜?
애니메이션이 너무 많으면 로딩 속도가 현저히 낮아짐
어떻게?

// Mesh용 정점 구조체 선언 - Engine_Struct.h
VTXMESH

Load할때
iFlag = PreTransform
메시들을 기준점인 뼈대에 배치하는 행위로 이해하면 될듯
해당 옵션을 사용하면 numAnimations 가 0이 되버림 ( 원래 존재하는 애니메이션을 로드하지 않았다 )
로컬 스페이스에 있던 자유 분방한 상태의 것들을 뼈대에 배치(행렬을 이미 곱해버림)
상태가 중복 적용되어버림
Assimp에서는 해당 옵션을 주게되면 아예 애니메이션 정보를 못읽게함 ( 움직이지마 )
(결론) 애니메이션 없는 모델이면 해당 옵션으로 사용

메쉬의 로컬 상태의 정점에 내가 원하는 사전 변환행렬을 파라미터로 받아서 곱해두는 방법도 존재
