2026-04-13(월)

# Navigation

## Gara 함수

- 해당 함수는 수업 때 보여주기 위한 함수
- 에디터에서 구현해야함

## Initialize_Prototype

- 삼각형 단위로 ReadFile하면서 CCell을 만들어낸다.

## NAVIGATION_DESC

- 현재 이 네비게이션을 이용하려는 객체가 어떤 셀 안에 있다라는 것을 반환
- 아예 이 객체는 몇번째 셀에 위치해 있을 것이다 라는 것을 명시한다. 라고 생각해도 될듯

### 셀의 높이를 계산해서 객체를 셀 위에 태운다.

## 지형에 Navigation을 컴포넌트로 추가

- 지형을 그릴 때 Navigation도 같이 그려지게 하는 의도?

### Initialize(void\* pArg)

- CurrentIndex == -1 : 지형
- 지형의 WorldMatrix를 담는다.

# Player에 Navigation 소지하게함

## CPlayer::isMove(\_fvector vResultPos)

### 수직 벡터

x 와 z의 성분을 바꾼 뒤 부호를 바꾼다.

# 이웃 삼각형 설정

- 이중 루프

- XMVector3Equal()
- XMVectorEqual()
  차이점 알아둘것
