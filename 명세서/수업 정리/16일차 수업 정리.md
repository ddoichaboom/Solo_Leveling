2026-04-07(화)

# Weapon 클래스

## 설계

- PartObject 상속 관계
- WEAPON_DESC

- 플레이어를 따라다니기도 하지만, 뼈에도 고정을 시켜야한다 : pSocketBoneMatrix

- 무기의 scale 조정
  무기 상태 변경 -> SocketBone 행렬 변경

# SkyBox

# RenderState

- RenderState -> RSSetState, ...
- 셰이더로 세팅
  D3D11_RASTERIZER_DESC 구조체
  한곳에서 설정을 하고 Default로 되돌린다라는 개념이 아니라 모든 셰이더에 Default로 설정해줘야함
  .hlsli 파일 : 셰이더에서 사용할 수 있는 별도 파일? 헤더 역할 처럼

D3D11_DEPTH_STENCIL_DESC
