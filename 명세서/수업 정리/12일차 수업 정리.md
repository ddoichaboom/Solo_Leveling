2026-04-01(수)

- 메시에게 영향을 주는 뼈 인덱스 정보를 메시에 보관
- 각 메시에 내 모델의 전체 뼈 인덱스 중 어떤 뼈가 영향을 주는지, 몇 번째 뼈 인덱스인지 정보를 담아두고 셰이더로 던져준다.

```
pAIBone->mOffsetMatrix
```

한 세트의 뼈를 여러 모델이 공유할 수 있다. 라는 개념인듯?
근데 이제 모델마다 scale이나 위치 정보가 다를 수 있어서 해당 보정 정보를 여기서 가져오는듯?
사전 변환행렬 역할
CombinedTransform은 매 순간 갱신되는거지만 해당 mOffsetMatrix는 한번 정해지면 안바뀌기 때문에 나중에 두 개를 곱해서 결정

애니메이션 = 뼈들이 들고 있는 TransformMatrix를 갱신해준다.

Mesh 안에 Mesh의 이름을 멤버 변수로 저장

모델의 뼈를 살펴보면 메시의 이름과 같은 뼈가 존재한다.
해당 뼈의 행렬을 같은 이름의 메시에 곱해준다. 그럼 해당 뼈에 붙어서 나옴

Bone이 없던 애들은 Weight 값이 다 0이므로 셰이더에서 Weight값을 1로 최소값을 잡아준다.
