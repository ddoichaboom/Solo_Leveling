# 4월 7일 진행 사항

## 개요

Layer 1 (모델 파이프라인) Engine 측 클래스 작업 — **CAnimation 완성**.
이전 세션에서 Animation.h 골격만 작성하고 종료된 상태에서 이어받아, ANIMATION_DESC 도입 → Channel/Animation 시그니처 정비 → Animation 구현 → 빌드 성공까지 마무리.

---

## 완료 항목

### Step 1: Engine_Struct.h에 CHANNEL_DESC / ANIMATION_DESC 추가

#### 설계 결정 — DESC 패턴 채택
- **근거**: 기존 MESH_DESC와 컨벤션 통일. Animation은 채널 가변 배열 + 4개 이상 필드라 직접 파라미터 방식보다 DESC 묶음이 명확
- **이유**:
  1. MESH_DESC와 일관성 (Engine_Struct.h 내부 컨벤션 통일)
  2. 계층 구조(Animation→Channels→KeyFrames)와 자연스럽게 매핑
  3. 향후 바이너리 fread에서 구조체 그대로 매핑 가능 (목차 7 대비)
  4. Editor → Engine 핸드오프가 한 호출로 끝남
  5. 필드 추가 시 시그니처 폭발 방지

#### raw pointer 방식 채택 (vector 아님)
- **이유**: MESH_DESC가 이미 raw pointer 패턴(`pVertices`, `pIndices`, `pBoneIndices`). 바이너리 fread 직결성 + Engine_Struct.h 컨벤션 일관성

#### 추가 위치
KEYFRAME 정의 직후, PICK_DATA 앞 (CHANNEL_DESC가 KEYFRAME 참조)

#### 추가 코드
```cpp
typedef struct tagChannelDesc
{
    unsigned int    iBoneIndex      = {};
    unsigned int    iNumKeyFrames   = {};
    KEYFRAME*       pKeyFrames      = { nullptr };       // 호출자 소유, Channel 내부 복사
}CHANNEL_DESC;

typedef struct tagAnimationDesc
{
    char            szName[MAX_PATH] = {};
    float           fDuration        = {};
    float           fTickPerSecond   = {};
    unsigned int    iNumChannels     = {};
    CHANNEL_DESC*   pChannels        = { nullptr };      // 호출자 소유
}ANIMATION_DESC;
```

#### 메모리 수명
```
[Editor] Assimp aiAnimation 파싱
  → ANIMATION_DESC 생성 (KEYFRAME[]/CHANNEL_DESC[] 임시 할당)
  → CAnimation::Create(ANIMATION_DESC&)
       └ CChannel::Create(CHANNEL_DESC&) 반복
            └ 내부 vector로 deep copy (memcpy)
  → ANIMATION_DESC + 임시 배열 모두 해제 (Editor 책임)
```

---

### Step 2: Channel.h / Channel.cpp 시그니처 수정

#### 변경
- `Initialize(_uint, const vector<KEYFRAME>&)` → `Initialize(const CHANNEL_DESC&)`
- `Create(_uint, const vector<KEYFRAME>&)` → `Create(const CHANNEL_DESC&)`
- 멤버 변수, Update_TransformationMatrix는 변경 없음

#### Initialize 본문
```cpp
HRESULT CChannel::Initialize(const CHANNEL_DESC& Desc)
{
    m_iBoneIndex    = Desc.iBoneIndex;
    m_iNumKeyFrames = Desc.iNumKeyFrames;

    if (m_iNumKeyFrames > 0)
    {
        m_KeyFrames.resize(m_iNumKeyFrames);
        memcpy(m_KeyFrames.data(), Desc.pKeyFrames, sizeof(KEYFRAME) * m_iNumKeyFrames);
    }

    return S_OK;
}
```

#### 결정사항
- **m_KeyFrames는 vector<KEYFRAME> 유지** — 멤버를 raw 배열로 바꾸면 Update 인덱싱 코드 영향 범위 큼
- **memcpy** — KEYFRAME이 POD라 안전하고 가장 빠름
- **0개 가드** — `if (m_iNumKeyFrames > 0)`. 0개여도 fail 아님 (Update에서 처리됨)

---

### Step 3: Channel.cpp Update_TransformationMatrix를 수업 코드 방식으로 변경

#### 변경 사항 (Solo_Leveling 기존 → 수업 코드 방식)
1. **결과 변수 타입**: `_float3/4` → `_vector` (Store→Load 왕복 제거, SIMD 효율)
2. **Translation W=1**: 보간 분기에서 미리 `XMVectorSetW(..., 1.f)` 설정
3. **브래킷 탐색**: `while (idx < N-1) { if (...) break; }` → `while (track >= keyFrames[idx+1].pos) ++idx;` (수업 코드 방식, boundary 체크 없이 가드 ②에 의존)
4. **루프 리셋 가드 추가**: `if (0.f == fCurrentTrackPosition) *pCurrentKeyFrameIndex = 0;`
5. **가드 ③ 유지**: `else if (track <= front().fTrackPosition) → 첫 값 고정`

---

### Step 4: Animation.h 선언

#### 멤버 / 시그니처 확정
```cpp
class CAnimation final : public CBase
{
private:
    CAnimation();
    CAnimation(const CAnimation& Prototype);
    virtual ~CAnimation() = default;

public:
    const _char*    Get_Name() const                { return m_szName; }
    _float          Get_Duration() const            { return m_fDuration; }
    _float          Get_TickPerSecond() const       { return m_fTickPerSecond; }
    _float          Get_CurrentTrackPosition() const{ return m_fCurrentTrackPosition; }
    void            Reset_TrackPosition()           { m_fCurrentTrackPosition = 0.f; }

public:
    HRESULT         Initialize(const ANIMATION_DESC& Desc);
    _bool           Update_TransformationMatrix(const vector<class CBone*>& Bones,
                                                _float fTimeDelta, _bool isLoop);

private:
    _char           m_szName[MAX_PATH] = {};
    _float          m_fDuration = {};
    _float          m_fTickPerSecond = {};
    _float          m_fCurrentTrackPosition = {};

    _uint                       m_iNumChannels = {};
    vector<class CChannel*>     m_Channels;
    vector<_uint>               m_CurrentKeyFrameIndices;

public:
    static CAnimation*  Create(const ANIMATION_DESC& Desc);
    CAnimation*         Clone();
    virtual void        Free() override;
};
```

#### 결정사항
- **szName 멤버 추가** — Editor 클립 선택 UI, 디버그 로그용
- **Getter 5개** — Layer 1 후반 / Layer 2 Inspector에서 사용
- **Reset_TrackPosition만 setter 형태로 노출** — 일반 setter는 외부 조작 위험
- **Set_CurrentTrackPosition 제외** — Editor 스크러빙은 YAGNI, 필요 시 추가
- **Update 함수명**: `Update_TransformationMatrix` (단수). 수업 코드는 복수형이지만 단일/복수 기능 차이 없어 단수 채택
- **Clone 깊은 복사 범위**: Channel은 공유(AddRef), 재생 상태(`m_fCurrentTrackPosition`, `m_CurrentKeyFrameIndices`)만 인스턴스별 독립
  - KEYFRAME 데이터 자체는 불변이라 공유가 정답
  - 한 모델이 30개 애니, 각 70개 본 → 2100개 Channel을 인스턴스마다 복제하면 메모리 낭비 + 캐시 비효율

---

### Step 5: Animation.cpp 구현

#### 핵심 구현
```cpp
HRESULT CAnimation::Initialize(const ANIMATION_DESC& Desc)
{
    strcpy_s(m_szName, Desc.szName);
    m_fDuration         = Desc.fDuration;
    m_fTickPerSecond    = Desc.fTickPerSecond;
    m_iNumChannels      = Desc.iNumChannels;

    m_Channels.reserve(m_iNumChannels);

    for (size_t i = 0; i < m_iNumChannels; i++)
    {
        CChannel* pChannel = CChannel::Create(Desc.pChannels[i]);
        if (nullptr == pChannel)
            return E_FAIL;

        m_Channels.push_back(pChannel);
    }

    m_CurrentKeyFrameIndices.resize(m_iNumChannels);

    return S_OK;
}

_bool CAnimation::Update_TransformationMatrix(const vector<CBone*>& Bones, _float fTimeDelta, _bool isLoop)
{
    m_fCurrentTrackPosition += m_fTickPerSecond * fTimeDelta;

    if (m_fCurrentTrackPosition >= m_fDuration)
    {
        if (false == isLoop) return true;
        m_fCurrentTrackPosition = 0.f;
    }

    _uint iChannelIndex = {};
    for (auto& pChannel : m_Channels)
        pChannel->Update_TransformationMatrix(Bones, m_fCurrentTrackPosition, &m_CurrentKeyFrameIndices[iChannelIndex++]);

    return false;
}
```

#### Animation `= 0.f` ↔ Channel `if (0.f == ...)` 가드의 정합성
- 루프 리셋 직후 Channel이 트랙 0을 받음 → Channel-side 가드가 keyFrameIndex를 0으로 리셋
- **두 코드가 짝으로 정확히 동작**. 한 프레임만 가드 발동, 다음 프레임부터는 정상 보간 분기

#### 대안 (선택, 미적용)
- `m_fCurrentTrackPosition -= m_fDuration` 방식 — 트랙 잔량 보존, track > 0 항상 보장. 이 경우 Channel의 `if (0.f == ...)` 가드는 죽은 코드가 됨
- 더 정밀하지만 현재 `= 0.f` 방식도 정상 동작이라 변경하지 않음

---

### Step 6: vcxproj 등록 확인 — 이미 완료 상태

5쌍 모두 Engine.vcxproj에 등록되어 있음:
```
ClCompile: Animation.cpp, Bone.cpp, Channel.cpp, Material.cpp, Mesh.cpp
ClInclude: Animation.h, Bone.h, Channel.h, Material.h, Mesh.h
```

→ 빌드에서 실제로 컴파일됨. 빌드 성공 = 5개 클래스 모두 정상 컴파일.

---

## Channel.cpp 가드 4개 의미 정리 (논의 결과)

| 가드 | 발동 조건 | 의도 |
|------|-----------|------|
| ① `iNumKeyFrames == 0` | 빈 채널 | 안전장치. division by zero / out-of-bounds 방지 |
| ② `track >= back().fTrackPosition` | 마지막 키프레임 시간 초과 | 외삽 방지. 마지막 값 고정. 비루프 종료 시 자연스러운 마지막 포즈 유지 |
| ③ `track <= front().fTrackPosition` | 첫 키프레임 시간 미만 | 역방향 외삽 방지. 첫 값 고정. 첫 키프레임이 시간 0이 아닌 경우(드묾) 또는 정확히 일치 |
| ④ `else` | 두 키프레임 사이 | 정상 보간. 브래킷 탐색 + Lerp/Slerp |

### 가드 ③ 관련 논의 결과
- 사용자 우려: "가드 ③ 살리면 첫 키프레임 이전 구간에서 보간 안 됨"
- **사실 확인**: 정확함. `[0, front().fTrackPosition]` 구간 내내 첫 값 고정
- **하지만**: 현재 Animation의 트랙 누적 방식(`+= tickPerSecond * dt` 후 사용)이라 **track > 0이 항상 보장됨** (예외: 루프 wraparound 한 프레임)
  - 첫 키프레임이 시간 0 (Assimp 일반적 케이스)인 경우: 가드 ③이 실제로 발동하지 않음 → 정지 구간 0
  - 첫 키프레임이 시간 > 0 (드묾)인 경우: 정지 구간 발생
- **결론**: 가드 ③ 유지가 표준이고 안전. 만약 첫 키프레임 ≠ 0인 데이터가 발견되면 **Editor 변환 단계에서 시간 0으로 정규화**가 진짜 해법
- 가드 ③ 제거 시 음수 비율 보간 → Scale 음수, Translation 반대 방향, Slerp 정의되지 않음 → **시각적 깨짐**

---

## 트러블슈팅

### 1. Channel.cpp `vTranslation` 콤마 typo (사용자 작성 단계)

#### 증상
보간 분기의 마지막 줄:
```cpp
vTranslation, XMVectorLerp(vSrcTranslation, vDstTranslation, fRatio);
```

#### 원인
`=`가 아닌 **콤마 연산자 표현식**. `vTranslation` 평가(부수효과 0) → Lerp 결과 평가(버려짐). 결과적으로 vTranslation은 `_vector{}` (0,0,0,0) 그대로.

#### 잠재적 시각 증상
보간 분기 진입 시 모든 본의 로컬 translation이 (0,0,0)으로 덮어씌워짐 → 캐릭터가 한 점으로 찌부러진 채 애니메이션 재생.

#### 해결
```cpp
vTranslation = XMVectorLerp(vSrcTranslation, vDstTranslation, fRatio);
```

### 2. Animation.cpp 복사 생성자 brace-init 함정 (사용자 작성 단계)

#### 증상
```cpp
, m_CurrentKeyFrameIndices { Prototype.m_iNumChannels }
```

#### 원인
C++ vector의 brace-init은 `initializer_list<T>` 생성자를 우선 매칭. `{N}` (size_t 한 개)을 넘기면:
- 의도: size N짜리 vector 생성
- 실제: **size 1짜리 vector, 첫 원소가 N**

#### 잠재적 런타임 증상
m_iNumChannels = 70 → m_CurrentKeyFrameIndices는 size 1, 첫 원소 70.
Update에서 `&m_CurrentKeyFrameIndices[iChannelIndex++]` 인덱싱 → out-of-bounds, 메모리 손상, 즉시 크래시.

#### 해결 (사용자가 다른 방식으로 수정)
```cpp
, m_CurrentKeyFrameIndices { Prototype.m_CurrentKeyFrameIndices }
```
**vector → vector 복사**는 brace-init이라도 `vector(const vector&)` 복사 생성자가 우선 매칭됨. 정상 동작. 이전 `{m_iNumChannels}`와 다른 패턴이 됨.

(대안: `m_CurrentKeyFrameIndices(Prototype.m_iNumChannels, 0)` 소괄호 사용)

### 3. Animation.cpp Create / Clone / Free 구현 누락 (사용자 작성 단계)

#### 증상
- `Create`: `return nullptr;` 한 줄만
- `Clone`: 본문 비어있음, return 없음
- `Free`: 본문 비어있음

#### 잠재적 영향
- Create: 모든 Animation 생성 호출이 nullptr 반환 → Animation 자체가 작동 안 함
- Clone: 정의되지 않은 동작 (스택 쓰레기 반환), C4715 경고
- Free: 채널 RefCount 감소 안 함 → 모든 채널 영구 누수

#### 해결
표준 패턴으로 모두 구현:
```cpp
CAnimation* CAnimation::Create(const ANIMATION_DESC& Desc)
{
    CAnimation* pInstance = new CAnimation();
    if (FAILED(pInstance->Initialize(Desc)))
    {
        MSG_BOX("Failed to Created : CAnimation");
        Safe_Release(pInstance);
    }
    return pInstance;
}

CAnimation* CAnimation::Clone() { return new CAnimation(*this); }

void CAnimation::Free()
{
    __super::Free();
    for (auto& pChannel : m_Channels)
        Safe_Release(pChannel);
    m_Channels.clear();
}
```

---

## Layer 1 Engine 측 진행 상태

| 항목 | 상태 |
|------|------|
| Engine 구조체 (VTXMESH, VTXANIMMESH, KEYFRAME, MESH_DESC, CHANNEL_DESC, ANIMATION_DESC) | 완료 |
| CBone | 완료 |
| CChannel | 완료 |
| CAnimation | 완료 (오늘) |
| CMesh | 완료 |
| CMaterial | 완료 |
| **CModel** (위 5개를 묶는 컨테이너) | 미작성 |
| Editor Assimp 통합 (목차 6) | 미진행 |
| 바이너리 포맷 (목차 7) | 미진행 |
| Shader_VtxAnimMesh.hlsl (목차 8) | 미진행 |

---

## 사소한 미해결 항목 (다음 작업 시 정리)

### Animation.cpp 복사 생성자: m_fCurrentTrackPosition Prototype 값 복사
```cpp
, m_fCurrentTrackPosition { Prototype.m_fCurrentTrackPosition }
```
신규 인스턴스는 0부터 시작이 더 명확. 권장:
```cpp
, m_fCurrentTrackPosition { 0.f }
```
일반적으로 Prototype은 Update를 안 받으므로 실질 영향은 없음. CModel 작성 시 Prototype 패턴 정리하면서 함께 손보는 것 권장.

---

## 다음 진행 사항

### 우선순위 1: CModel 골격 작성
- **이유**: 이미 만든 5개 클래스(Bone/Channel/Animation/Mesh/Material)는 모두 CModel이 vector로 들고 다니는 부속품. CModel 없이는 외부에서 한 묶음으로 다룰 방법 없음
- **범위**: 우선 raw 데이터(메모리) 초기화 경로만. 바이너리 fread 경로는 목차 7에서

#### CModel 설계 (4월 6일 메모 기반)
```
CModel (CComponent 상속)
├── vector<CMesh*>        — 메시 배열
├── vector<CMaterial*>    — 머터리얼 배열
├── vector<CBone*>        — 본 배열
└── vector<CAnimation*>   — 애니메이션 클립 배열

핵심 멤버: m_eType(MODEL), m_iCurrentAnimationIndex, m_PreTransformMatrix
Initialize 호출 순서: ① Ready_Bones → ② Ready_Meshes → ③ Ready_Materials → ④ Ready_Animations
  (본이 먼저 만들어져야 Mesh/Animation이 Get_BoneIndex()로 본 검색 가능)
Clone 전략: Mesh/Material 공유(AddRef), Bone/Animation 깊은 복사
```

### 우선순위 2: Editor Assimp 통합 (목차 6)
- Assimp 라이브러리 세팅 (Editor에만 격리)
- Assimp aiScene → Engine 구조체 (MODEL_DESC, MESH_DESC, ANIMATION_DESC, CHANNEL_DESC) 변환
- Editor에서 FBX 로드 → CModel 생성 → Editor Viewport 렌더링 검증

### 우선순위 3: 바이너리 포맷 설계 (목차 7)
- 모델/메시/머터리얼/본/애니메이션을 단일 바이너리로 직렬화
- Editor Export, 런타임 fread

### 우선순위 4: 셰이더 (목차 8)
- Shader_VtxAnimMesh.hlsl (스키닝)
